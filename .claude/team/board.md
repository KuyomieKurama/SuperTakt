# Aufgabenboard — Takt

Stand: 2026-09-01, Abschluss des Durchlaufs. 55 Aufgaben, 55 Berichte.

Nachtrag T-053 bis T-055: Takt startete nicht — der gebündelte Sidecar starb beim Start, weil im
Bündel weder Quelltextort noch Migrationsverzeichnis existieren. Elf Nachweispfade waren grün,
weil **jeder von ihnen aus dem Quelltext lief**. Behoben; der Sidecar-Nachweis wuchs von 12 auf
20 Prüfungen, läuft jetzt in `pnpm desktop` mit, und die Warnung, die den Fehler die ganze Zeit
angesagt hatte, bricht den Bau. Für Oberfläche und Add-in wurde gemessen, dass dieselbe Lücke
dort **nicht** klafft — mit Gegenproben. Und das Bündel des Add-in-Aufgabenbereichs wird jetzt
überhaupt erst ausgeliefert.

---

## Fertig

Alle Aufgaben von T-001 bis T-052. Berichte unter `.claude/team/reports/`.

| Bereich | Ergebnis |
|---|---|
| Domäne und Speicherung | 17 Tabellen, 7 Migrationen vorwärts und rückwärts geprüft, Abdeckung 100 Prozent |
| Lokaler Dienst | 64 Operationen hinter Nachweisprüfung, Export transaktional und gemessen |
| Oberfläche | 14 Screens gegen den echten Dienst, 332 Kontrastpaare ohne Durchfaller |
| Tauri-Hülle | `.deb` und `.AppImage`, 22 Rust-Tests, Sidecar mit Lebenszyklus |
| Outlook-Add-in | 86 Prüfungen, konfigurierbarer regulärer Ausdruck, Duplikatangebot |
| Exportmotor | Vorlagen mit geschlossener Quellenliste, ein Renderer für Vorschau und Datei |
| Tests | 556 Unit- und Integrationsfälle, 28 End-to-End-Fälle, 11 Nachweispfade |
| Dokumentation | Benutzer- und Entwicklerhandbuch, Glossar, README |
| Prüfberichte | Bedrohungsmodell (52 Bedrohungen), Testplan (125 Fälle), Zustandsmatrix (14 Screens) |

Urteile: Spezifikations- und UX-Abgleich **freigegeben** (T-042). Sicherheitsprüfung und
Code-Review haben je vier blockierende Befunde gemeldet, alle behoben.

## Läuft — Welle vom 2026-09-03

Ausgangspunkt: T-076 hat die Regel von einer Liste gleichartiger Terme auf eine **Struktur mit
benannten Feldern** umgebaut (E-055, Vorbild Super Productivity): erforderliche Tags mit Modus,
ausgeschlossene Tags, Status, Erledigt, Exportstatus. Alle dreizehn Nachweispfade grün.
`pnpm check` rot an genau einem Punkt — Zweigabdeckung `packages/storage/src` 79,54 gegen 80.

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-077~~ | **fertig.** `pnpm check` Exitcode 0. Zweigabdeckung Speicherung 79,55 auf 81,68, Domäne 80,00 auf 85,29 | unit-tester |
| ~~T-078~~ | **fertig.** `poolNamer` behoben, E-056 gebaut, `proof:addin` 100 auf 112 | integration-dev |
| ~~T-079~~ | **fertig.** Regelformular, `PoolFormDialog` 236 auf 604 Zeilen, Kontrast 416 Paare ohne Durchfaller | frontend-dev |

Alle drei fertig, committet als `48c982a`. `pnpm check` grün.

**Dabei gefunden und behoben — dieselbe Lücke wie T-053:** `pnpm check` lief nur **vier der
dreizehn** Nachweispfade. Die anderen neun waren nur im Unterpaket erreichbar und hingen in keiner
Kette. Alle dreizehn hängen jetzt an `proof:all`, dazu `verify:bundle`: 770 Prüfungen in 37
Sekunden, ganzer Durchlauf 71.

### Welle vom 2026-09-03, zweite Runde

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-080~~ | **fertig.** Es waren **drei** Fassungen, nicht zwei — Domäne, SQL, Oberfläche. Alle fragen `poolRuleIsEmpty`; sechste Achse gibt sechs Typfehler in drei Paketen. Committet als `a2d74ef` | domain-dev |

**Nebenbefund aus T-080, schwerer als der Anlass:** Ein Ordnerterm über einen leeren Ordner
verschwindet in `matchesPool` als Neutralwert. „Tags aus Ordner X **und** Status offen" wird zu
„Status offen" — die Regel trifft **mehr**, als der Benutzer gesagt hat. Entschieden als **E-057**:
Einschränkung ohne Treffer, die Regel trifft nichts.

### Welle vom 2026-09-03, dritte Runde

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-082~~ | **braucht Review.** `poolRuleMatchesNothing` in Domäne und SQL, termweise; `unresolvedRequired` Pflicht; `resolved.emptyRuleFolderIds`. Nebenfund: `buildConditions` hängte Parameter an, bevor feststand, ob ihre Bedingung im Text landet — mit E-057 wären alle folgenden Werte verrutscht. Behoben. Kein bestehender Prüffall wäre rot geworden (beide Seiten hatten denselben Fehler); `pnpm check` rot an genau `routes/addin/service.ts:326` | domain-dev |
| ~~T-083~~ | **fertig.** `countConditions` gelöscht, Domäne gefragt; drei Leerzustände an sechs Flächen. Befund: `resolved.tagCount` ist achsenweise, E-057 termweise — im gemischten Fall unsichtbar. An T-082 weitergegeben: `emptyRuleFolderIds` | frontend-dev |
| ~~T-084~~ | **fertig.** Dritte Liste `enters` neben `appears`/`leaves`, `proof:addin` 112 auf 117. Befund: `matchesPool` hat ein neues **freiwilliges** Feld, `poolNamer` gibt es nicht mit — T-078-Falle ein zweites Mal. An T-082: Feld wird Pflicht | integration-dev |
| ~~T-085~~ | **fertig.** `forbidOnly` greift — nachgewiesen mit temporärem `.only` unter `CI=1`. Erster Lauf 30/3/1 durch Last paralleler Agenten, zweiter 34/34 | e2e-tester |

### Welle vom 2026-09-03, vierte Runde (Nachwelle zu T-082)

Vorab durch den Orchestrator: Testdateien wurden von `pnpm typecheck` nicht erfaßt — die
Paket-`tsconfig`s stehen auf `include: ["src"]`, Vitest prüft keine Typen. Das Pflichtfeld
`unresolvedRequired` erzwang in den Tests also nichts. Neu: `tsconfig.test.json` in domain,
storage, export und `typecheck:test` in `typecheck`. Erste Messung: 62 Typfehler in Tests, davon
31 fehlende `unresolvedRequired` in `board.test.ts`/`tags-and-pools.test.ts` und einer in der
Kreuzprüfung `repo-todos.test.ts:719` — die lief bislang mit `undefined`.

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-086~~ | **fertig.** `poolNamer` über `resolveAxes`, ein Portaufruf statt zweier; `resolveRule`/`resolveExcluded` aus dem Add-in-Ausschnitt entfernt (kein Aufrufer mehr). `proof:addin` 117 auf 123, drei Mutationen gegengemessen. `pnpm run typecheck` Exitcode 0. Nachtrag für domain-dev: Kommentar an `ports.ts` nennt einen Aufrufer in `routes/addin`, den es nicht mehr gibt | integration-dev |
| ~~T-087~~ | **fertig.** `describeRuleReach` über `unresolvedRequired` + `emptyRuleFolderIds`, Chip statt Achse markiert; `unresolvedExcluded` als Hinweis. `matchesNothing` bewusst nicht als Ganzes gelesen (vermischt Regelterme mit allen Achsen — im Formular stünde „trifft nichts" am Entwurf). Vorschlag an domain-dev: `matchesNothingReason` als Aufzählung statt weiterer Wahrheitswerte. E2E 34/34 | frontend-dev |
| ~~T-088~~ | **fertig.** `typecheck:test` 62 auf 0; vier T-082-Fälle je als reine Funktion und gegen SQL, mit Gegenprobe und bewusst falscher Vergleichsfunktion statt Gleichheitstest. 595 Fälle grün, 80 % gehalten. Nebenfund: `pnpm --filter <paket> test` lief lautlos leer (kein Skript) — Orchestrator hat `test`-Skripte in den drei Paketen nachgetragen | unit-tester |

| ~~T-081~~ | **fertig.** 3 gegenstandslose Fälle gelöscht, 4 neue; dazu 2 Fälle in anderen Dateien repariert, die dieselbe Umstellung gebrochen hatte. **34/34** | e2e-tester |

T-080 wurde mit HTTP 529 abgebrochen und per Nachricht fortgesetzt, nicht neu gestartet.

**Dabei gefunden — wieder ein Nachweis, der nur von Hand lief:** `pnpm test:e2e` benutzte den
Wurzel-`playwright.config.ts` ohne `globalSetup`, startete keine Dienste, und **alle 34 Fälle
scheiterten sofort**. Die echte Konfiguration lag seit T-012 in `tests/e2e/`, mit dem richtigen
Aufruf im Dateikopf dokumentiert — den man kennen musste. Wurzel-Config gelöscht, `test:e2e`
zeigt auf die echte. Gemessen: 34 passed über `pnpm test:e2e`.

Kollisionsfrei: `packages/*/test/**` gegen `routes/addin/**` gegen `apps/web/**`.
End-to-End erst danach — `tests/e2e/kanban.spec.ts` prüft noch das Ziehen, das E-054 abgeschafft
hat.

### Welle vom 2026-09-03, Review-Runde (Qualitätstor)

Stand `3240dcc`: `pnpm check` Exitcode 0 (13 Nachweispfade, 818 Prüfungen, 595 Einheitentests,
Abdeckung 91 %), `pnpm test:e2e` 34/34. Prüfumfang `git diff 7c71186..HEAD`.

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~R-1~~ | **nicht freigegeben.** Blockierend: (1) `TagFolderPort.remove` prüft keine Verwendung in `pool_rule`, `folder_id` steht auf `CASCADE` — Löschen eines leeren Ordners macht „Ordner Ost und Status offen" still zu „Status offen", genau die Richtung gegen E-057; (2) `bookOnTodo` gibt bei scheiterndem `clearDone` `rejected` **zurück** statt abzubrechen, die Unit of Work committet — Buchung steht, Client hört „abgewiesen", Doppelbuchung; (3) `poolNamer` ruft `pools.list()` ohne `'all'`, Attrappe ignoriert das Argument — deckt sich mit R-2 B-4; (4) 138,7 MB AppImage + 47,5 MB `.deb` unter `apps/desktop/release/` in `48c982a`, `.git` 181 MB. Sollte: Typwache lückenhaft in `board.ts:86` und `service.ts:306` (sechste Achse würde dort nicht rot), `as never` in `structure.ts`, zweite Fassung der Filteraufzählungen in `labels.ts`, `status_in_use` ohne vierten Grund in OpenAPI | code-reviewer |
| ~~R-2~~ | **nicht freigegeben.** B-1: `CARD_STAYS` („die Spalte ändert sich dadurch nicht") ist seit E-055 falsch, zeichengleich an vier Flächen, seit T-072 offen. B-2: Add-in nennt `leaves`, Hauptanwendung nicht (C-03). B-4: `poolNamer` ruft `pools.list()` ohne `'board'` — reine Board-Spalten nie genannt. B-5: `PoolFormDialog` ohne Lade-/Fehlerzustand für Ordner und Status. D-1/D-2: Benutzerhandbuch beschreibt Drag & Drop. Deckungslücken A-3.5/A-3.6/A-5.7 nachzutragen. Empfehlung O-E: kein Ziehen, Status-Untermenü auf der Karte; O-K: Wahrheitswert über den ganzen Satz | spec-ux-reviewer |
| R-3 | Sicherheitsprüfung: Validierung der Regelfelder, SQL-Zusammensetzung, Vertrauensgrenze Add-in nach `resolveAxes`, Notiz-Trennung, `RESTRICT`, Repository-Hygiene | security-checker |

Danach documenter.

### Welle vom 2026-09-03, Reparatur nach Review — Welle A

Vorab durch den Orchestrator: 186 MB Bauergebnisse (`apps/desktop/release/`) und die
Lizenzbeilage aus den fünf Commits des Branches genommen (`git filter-branch`, Sicherung
`backup/status-als-regelterm-vor-filter`), `apps/desktop/.gitignore` wörtlich von
`release-workflow`. E-058 festgehalten. Doppelte O-Kennungen umbenannt (O-M bis O-Q).

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-089~~ | **fertig, braucht Review (R-1a).** Migration 0012 `pool_rule.tag_id`/`folder_id` CASCADE → RESTRICT, `TagFolderPort.remove` weist mit `tag_in_use` + Regelnamen in `details` ab (gleicher Zusatz an `status_in_use`, H-2); `check-export-boundary.mjs` Untergrenzen; `z.array(idSchema).min(1).max(50)` für die drei Kennungslisten von `GET /todos`; `BoardColumnRule extends MatchesPoolRule`; `structure.ts` ohne `as never`; `matchesPool` wirft bei fehlendem `unresolvedRequired`. E-058: `usecases/pool-movement.ts` (`poolMovementNamer`), `POST /timer/start` liefert `poolMovement \| null`, `poolMovementSentence(movement, tense, occasion)` in `@takt/domain` mit Überladungen. 595/595, typecheck 0. Offen (entschieden, siehe E-058 Ergänzung): Gattungswort → keins; Stopp/orphaned liefern ebenfalls `poolMovement`; `GET /addin/context` bleibt bei `list()`. R-3 H-1 (Ordnerauflösung Faktor 70) weiter beim Auftraggeber | domain-dev |
| ~~T-090~~ | **fertig.** `poolNamer` über `list('all')`, Attrappe wertet die Fläche aus, `proof:addin` 123 auf 131; `bookOnTodo` wirft `AbortBooking` statt `rejected` zurückzugeben — Doppelbuchung zu, Mutation gemessen; Typwache `NamedPoolRule` (lokal, in Welle B durch Domänen-Erbauer ersetzen); „Status" statt „Spalte". Offen: Gattungswort im Bewegungssatz für reine Board-Spalten (E-058/Welle B); Kommentar an `PoolPort.list` nennt `poolNamer` als Zeugen für die Vorgabe `'pool'` — stimmt nicht mehr (domain-dev) | integration-dev |
| ~~T-091~~ | **fertig.** B-5: Ordner- und Statusauswahl mit lädt/Fehler+Wiederholen/bereit (`RulePickers.tsx`), keine falsche Leermeldung mehr; B-3b: `slice(0,12)` aufgehoben; `labels.ts` über Domänentypen; „Regel über Tags" an 15 Stellen; `aria-describedby`; Live-Region an `RuleSummary` (500 ms); S-1, S-3, S-5 (schwache Fassung: kein Bestätigungsdialog, dafür „Rückgängig"), S-7, S-9, S-10, H-1–H-3; Ordnersuche (A-4.4 halb). typecheck 0, contrast 0/432, e2e 34/34. Offen: benannte Domänentypen für `AppSettings.theme`/`Pool.matchMode` (domain-dev); `POOL_EXPORT_LABEL.open` → „Noch nicht abgerechnet"? (Auftraggeber); Bestätigungsdialog „Vom Board nehmen" fallen lassen? (Auftraggeber); Kommentar `tests/e2e/support/actions.ts:125-131` veraltet (e2e-tester, Welle B) | frontend-dev |

**Messung nach Welle A (Orchestrator):** `pnpm check` — 13 Nachweispfade 838/0, 595 Einheitentests,
Zweigabdeckung `packages/domain/src` 69,72 % unter 80 % (allein `pool-movement.ts` mit 0 %, Tests
kommen in T-095) → Exitcode 1 an dieser einen Schwelle, sonst grün. `pnpm test:e2e` 34/34 grün (1,3 min).

### Welle vom 2026-09-03, Reparatur nach Review — Welle B

Vorab durch den Orchestrator: E-058 um die Punkte 4–7 ergänzt (kein Gattungswort im Satz;
`occasion` bleibt Pflicht; auch Stopp und `orphaned/resolve` liefern `poolMovement`;
`GET /addin/context` bleibt bei `list()`), E-059 (Exportstatus heißt in der Oberfläche „Noch
nicht abgerechnet"/„Abgerechnet"; „Vom Board nehmen" ohne Bestätigungsdialog, mit „Rückgängig").

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-092~~ | **fertig.** `poolNamer`/`bookingStates`/`NamedPoolRule` gelöscht, Add-in-Dienst bildet nur das Zustandspaar (`BOOKING_EFFECT` einmal) und ruft `poolMovementNamer`; `reopen.ts` ohne `poolSentence`/`bookingPoolSentence`/`CARD_STAYS`/`aside`, alles über `poolMovementSentence`; `proof:addin` erzeugt die Erwartung aus der Funktion, 134/0, drei statische Wachen (keine zweite Satzfassung, kein `CARD_STAYS`, kein `matchesPool` im Add-in-Dienst). typecheck 0, 648 Tests, fünf Mutationen rot. Offen: Zustandspaar „Wirkung einer Buchung" jetzt an vier Stellen — `bookingMovementStates(todo, entries)` in `usecases/pool-movement.ts` (domain-dev, O-S); Add-in-Routen liefern drei flache Listen, Timer-Routen `poolMovement` — Vereinheitlichung berührt zwei Hoheiten (O-T) | integration-dev |
| ~~T-093~~ | **fertig.** Satz nur mit Namen, 14/14 zeichengenau nach Tabelle; `POST /timer/stop` und `orphaned/resolve` liefern `poolMovement \| null` (Anlass Buchung, im verworfenen Zweig fest `null`); `PoolPort.list`-Kommentar richtiggestellt (O-I damit erledigt); `Theme`, `PoolMatchMode` exportiert. Alle Nachweispfade 0, `proof:openapi` 100, 633 Einheitentests, Zweigabdeckung Domäne 87,5 %. **Neuer Befund:** `orphaned/resolve` verspricht `reason: [timer_too_short, orphan_discarded]`, liefert nur `timer_too_short` — Entscheidung nötig (O-R) | domain-dev |
| ~~T-094~~ | **fertig.** `CARD_STAYS` und `poolsContaining` ersatzlos weg; Satz an jeder Fläche aus `poolMovementSentence` mit `poolMovement` aus `POST /timer/start`, Anlass aus `doneCleared`, `null` = keine Fläche (O-G damit erledigt: erste Buchung meldet die Spalte); E-059 bis in die Vorschau, `EXPORT_TEXT` gelöscht, Buchungsetikett aus der Regelachse genommen (Annahme 1, an R-2a); Musterseite zeigt vier Wiederöffnen-Fälle aus der Funktion. typecheck 0, build 0, contrast 0/432, boundaries 0, e2e 36 grün + 1 rot (`kanban.spec.ts:357` TP-KANBAN-06 von T-096, nicht T-094). Offen: `PUT`/`DELETE /todos/{id}/done` ebenfalls `poolMovement`? (O-U); Board-Toast nach „Erledigt" schweigt über Spalten, bis O-U entschieden | frontend-dev |
| ~~T-095~~ | **fertig.** 53 neue Fälle: 15 Wortfälle `poolMovementSentence` + Mehrfachaufzählung, Laufzeitwache `matchesPool`, `poolMovementNamer` (list('all'), enters/leaves-Invarianten, gleichnamige Pools über die Regel), Ordnersperre `tag_in_use` mit `details`, Migration 0012 vor/zurück mit Bestand, `describeRuleReach`/`emptyFolderNames` acht Fälle in `apps/web/test/lib/poolRule.test.ts`. 648/648, Domäne 87,5 % Zweige, `typecheck:test` über fünf Konfigurationen (Orchestrator: `apps/local-api/tsconfig.test.json`, `apps/web/tsconfig.test.json`) | unit-tester |
| ~~T-096~~ | **fertig.** TP-KANBAN-06 (E-057: leerer Ordner + Statusachse, Karte bleibt draußen, Vorschau nennt den Grund, Tag im Ordner füllt die Spalte); `tag-folder-rule-lock.spec.ts` (409 `tag_in_use` mit `details[]` über API und Oberfläche, Gegenprobe ungebundener Ordner); `actions.ts`-Kommentar berichtigt; `docs/testplan.md` Abschnitt 17. e2e 37/37. **Zwei Funde in `apps/web`:** (1) zweites `page.goto()` auf eine offene Route löst keine neue Anfrage aus (Daten veralten; im Test über `page.reload()` umgangen); (2) `TaktApiError.details` wird nirgends gelesen — Regelname bei `tag_in_use`/`status_in_use` erscheint nicht in der Fehlermeldung. Beides an frontend-dev, Welle C | e2e-tester |

Wortlaut nach E-058 Punkt 4 (Vorlage für T-092 bis T-095; `„X“` steht für einen Namen, mehrere
werden als `„A“, „B“ und „C“` aufgezählt):

| Anlass | Fall | Ankündigung (`future`) | Bericht (`past`) |
|---|---|---|---|
| reopen | nichts, nichts | Auf dieses Todo passt derzeit keine Regel — es erscheint danach in keinem Pool und in keiner Spalte. | Auf dieses Todo passt derzeit keine Regel, es erscheint also in keinem Pool und in keiner Spalte. |
| reopen | nur `leaves` | Es verschwindet dann aus „X“ und erscheint sonst nirgends. | Es ist aus „X“ verschwunden und erscheint sonst nirgends. |
| reopen | nur `appears` | Es erscheint dann wieder in „X“. | Es ist zurück in „X“. |
| reopen | beides | Es erscheint dann wieder in „X“ und verschwindet aus „Y“. | Es ist zurück in „X“ und aus „Y“ verschwunden. |
| booking | nichts, nichts | `null` | `null` |
| booking | nur `enters` | Es erscheint dann in „X“. | Es steht jetzt in „X“. |
| booking | nur `leaves` | Es verschwindet dann aus „X“. | Es ist aus „X“ verschwunden. |
| booking | beides | Es erscheint dann in „X“ und verschwindet aus „Y“. | Es steht jetzt in „X“ und ist aus „Y“ verschwunden. |

**Messung nach Welle B (Orchestrator):** `pnpm check` Exitcode 0 — 13 Nachweispfade 848/0, 648
Einheitentests, Zweigabdeckung gesamt 84,34 %; `pnpm test:e2e` 37/37.

### Welle vom 2026-09-04, Welle C

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-097~~ | **Fertig.** Stopp-Toast trägt den Satz aus `poolMovementSentence(…, 'past', 'booking')`; `discarded`-Zweig typisiert mit `poolMovement: null` (drei Stellen in `usecases/timer.ts` liefern dort ausnahmslos `null`). Neue `lib/errorText.ts` (`enumerateGerman`, `ruleReferences`, `errorMessageWithRules`) nennt Regelnamen aus `details[]` in den Löschdialogen von Ordner, Tag und Status. `page.goto()`-Fund ursächlich behoben: `hashchange` feuert bei gleichem Anker nicht, `popstate` schon — `useRoute` hört darauf und `useDataFreshness` lädt neu (auch bei `visibilitychange`); `navigate()` geht über `location.assign`, weil `location.hash = <gleich>` kein Ereignis erzeugt. Messung: `pnpm check` 0 (848/0, Zweige 84,34 %), e2e 37/37. Offen: `errorText.ts` ungetestet (unit-tester), `page.reload()` in `kanban.spec.ts` Zeilen 312/414 durch zweites `gotoBoard` ersetzbar (e2e-tester), Ordner-Löschdialog wechselt nach Absage Titel/Knopf nicht (hält sich an Selektor des e2e-Tests). Bericht `reports/T-097-frontend-dev.md`. Ursprünglich: Stopp-Antwort anbinden: `POST /timer/stop` und `POST /timer/orphaned/resolve` liefern `poolMovement \| null` (E-058 Punkt 6, OpenAPI ab T-093) — `performStop`/`confirmOrphan` in `TimerContext` bilden den Satz mit Anlass `'booking'`, `null` heißt keine Fläche; `api/types.ts` nachziehen. Funde aus T-096: (1) `TaktApiError.details` nirgends gelesen — bei `tag_in_use`/`status_in_use` die Regelnamen aus `details[]` in der Fehlermeldung nennen (Löschen-Dialog Ordner, Tag, Status); (2) zweites `page.goto()` auf eine offene Route löst keine neue Anfrage aus — Ursache finden (Router/Effekt-Abhängigkeit), beheben, damit `StructureContext`/Listen beim erneuten Aufruf frisch laden | frontend-dev |

| ~~T-098~~ | **Fertig** auf `fix/windows-sidecar-bundle-check` (von `origin/main`, gepusht, Commit 7390bc8; PR anlegen ist Sache des Auftraggebers), in `status-als-regelterm` gemergt (aca53df, damit ist auch 45c0d79 hier). `scripts/paths.mjs` mit `isInside(folder, file, path)` über `path.relative` — nicht `folder + sep`, weil `path.win32.resolve` den Laufwerksbuchstaben nicht vereinheitlicht, `path.win32.relative` aber ohne Rücksicht auf Groß-/Kleinschreibung vergleicht; 12 Fälle gegen `path.win32`/`path.posix` belegt. Zweiter Fund: `verify-node-checksums.mjs` `slice(repoRoot.length + 1)` → `relative`. Archivmitglieder (`split('/')`) bleiben, im Kommentar begründet. Unter Windows nicht ausgeführt — Nachweis ist der nächste Workflow-Lauf. Offen: Test für `isInside` unter `apps/desktop/test/` (unit-tester, Welle D). Ursprünglich: Windows-Build im Release-Workflow (`origin/main`, Lauf `pnpm desktop:build`): `build-sidecar.mjs` Schritt 2 bricht mit „Der lokale Dienst selbst ist nicht im Bündel" ab. Ursache (Orchestrator, Zeile 259): `input.startsWith(\`${folder}/\`)` — `join()`/`resolve()` liefern unter Windows Rückstriche, esbuild meldet die Eingaben mit Schrägstrichen relativ zum Arbeitsverzeichnis; nach `resolve` stehen Rückstriche gegen `…/` und kein Arbeitsbereichspaket zählt. Fix auf einem Zweig von `origin/main` (der Workflow liegt nicht auf `status-als-regelterm`, Commit 45c0d79 fehlt dort): Vergleich über `path.sep` bzw. `relative()`, dieselbe Durchsicht über alle Skripte in `apps/desktop/scripts/` (`collect-licenses`, `collect-release`, `build-app`, `sidecar-runtime`, `verify-node-checksums`), Nachweis der Vergleichsfunktion mit `path.win32` ohne Windows-Rechner. Läuft **nach** T-097, weil derselbe Arbeitsbaum die Zweige wechseln muss | frontend-dev |

### Welle vom 2026-09-04, Welle D (Qualitätstor, zweite Runde) — fertig

Stand `aca53df`: `pnpm check` Exitcode 0 (13 Nachweispfade 848/0, 648 Einheitentests, Zweige
84,34 %), `pnpm test:e2e` 37/37. Prüfumfang der Wiedervorlagen: `git diff 3240dcc..HEAD`
(Welle A, B, C und T-098) mit den Befunden aus R-1, R-2, R-3 als Ausgangspunkt.

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-099~~ | **Fertig.** Neu `tests/e2e/pool-movement-sentence.spec.ts` (Hauptanwendung gegen Add-in, reine Board-Spalte, `leaves`-Fall, kein-Treffer-Fall; Erwartung aus `poolMovementSentence` gezogen) und `tests/e2e/timer-stop-announcement.spec.ts` (`recorded`/`discarded`/`orphaned`-resolve; `discarded` zeitabhängig, zweimal ohne Zwischenfall). `tag-folder-rule-lock.spec.ts` prüft Regelname für Ordner/Tag/Status, `kanban.spec.ts` mit zweitem `gotoBoard()` statt `page.reload()`, `support/api.ts` erweitert (`createPool` mit `statusIds`/`exportState`, `startTimer`, `touchTimerHeartbeat`, Add-in-Helfer, typisierte Timer-Antworten), `docs/testplan.md` Abschnitt 17. 45/45. Fund bestätigt R-1a (1): `TagPort.remove()` liefert keine `details`, Tag-Dialog nennt die Regel nicht. Offen (Orchestrator): eigener `tsconfig`/Nachweispfad für `tests/e2e/**` — ein Typfehler wurde nur ad hoc gefunden. Ursprünglich: End-to-End: (1) Bewegungssatz Hauptanwendung gegen Add-in am E-056-Fall — dasselbe erledigte Todo, dieselbe reine Board-Spalte, Timerstart in der Oberfläche gegen Buchung über die Add-in-Route; beide Sätze zeichengleich bis auf die Zeitform (Tabelle im Board bei T-093); (2) Stopp-Anzeige: Toast nach `POST /timer/stop` trägt den Satz mit Anlass Buchung, gemessene Wortlaute in `reports/T-097-frontend-dev.md`; (3) `tag-folder-rule-lock.spec.ts` prüft jetzt den Regelnamen aus `details[]` im Dialogtext („Betroffen ist Regel „…“."); (4) `kanban.spec.ts` Zeilen 312/414: `page.reload()` durch zweites `gotoBoard(page)` ersetzen, Kommentare nachziehen — T-097 hat den Fund ursächlich behoben (`popstate`); (5) `docs/testplan.md` nachziehen | e2e-tester |
| ~~T-100~~ | **Fertig.** `apps/web/test/lib/errorText.test.ts` (22 Fälle, Prüfdaten aus `TaktApiError` und `poolReference`-Format in `mappers.ts`), `apps/desktop/test/paths.test.ts` (14 Fälle, `path.win32`/`path.posix`; erstes `test/` in `apps/desktop`, Import der `.mjs` mit `@ts-expect-error`). Rotnachweis über kurzzeitige Mutation der `src`-Dateien, per `git checkout --` zurück — **nicht wiederholen**, während andere Agenten parallel lesen oder messen. `useRoute`/`useDataFreshness` bewusst ohne Test (kein exportierter reiner Kern; jsdom bezeugt nur jsdom). 684 Einheitentests, `typecheck:test` 0, Abdeckung hält. Offen (Orchestrator): `tsconfig.test.json` für `apps/desktop`; `coverage.include` um `apps/web/src/lib`, `apps/desktop/scripts`? Ursprünglich: Einheitentests: `apps/web/src/lib/errorText.ts` (`enumerateGerman`, `ruleReferences`, `errorMessageWithRules`); `apps/desktop/scripts/paths.mjs` `isInside` unter `apps/desktop/test/` mit `path.win32` und `path.posix` (12 Fälle aus `reports/T-098-frontend-dev.md`); `useRoute`/`useDataFreshness` nur, wenn ohne Browser sinnvoll prüfbar | unit-tester |
| ~~R-1a~~ | **Freigegeben.** Vier Blockierer aus R-1 behoben und gemessen (Migration 0012 an roher DB mit Rückweg 7/7, `AbortBooking`, `list('all')` + Attrappe, 186-MB-Blobs aus allen Zweigen). E-058 hält: eine Rechnung, ein Satz, 14 Wortlaute zur Laufzeit zeichengleich, `null` überall „keine Fläche". Sieben wesentliche, keiner blockierend: (1) `repo-tags.ts:213` `TagPort.remove` ohne `details` — Ordner/Status liefern Namen, Tag nicht, Oberfläche liest schon (domain-dev); (2) `usecases/timer.ts:511` überschreibt `orphan_discarded` aus der Domäne mit `timer_too_short` — O-R: **unterscheiden, nicht kürzen** (domain-dev; `timer.ts:294`, OpenAPI `:2043` dazu); (3) `repo-tags.ts:192`, `repo-statuses.ts:277` CASCADE-Begründungen behaupten das Gegenteil der Lage (domain-dev); (4) `labels.ts:86/239` doppelte Aufzählungen, Bedingung für Import aus der Domäne inzwischen erfüllt (frontend-dev); (5) `verify-node-checksums.mjs:91` fängt nur null Einträge, Kopf verspricht sechs (frontend-dev); (6) `router.ts:134` Wiederbesuch hängt an `popstate` bei `location.assign` auf identische Adresse, nur Chromium gemessen, Linux-Hülle ist WebKitGTK — `navigate()` soll bei gleichem Ziel den Wiederbesuch selbst auslösen (frontend-dev); (7) `useDataFreshness.ts:73` `bump()` über `useAsync` verwirft den Wert → Platzhalter bei jedem Fensterwechsel, Kommentar sagt das Gegenteil (frontend-dev). O-S: Rechnung in `usecases/pool-movement.ts`, **Wirkung** (`BOOKING_EFFECT`) in `packages/domain`, mit O-T zusammen planen. Hoheitsverstoß T-100 notiert. Frage: `backup/status-als-regelterm-vor-filter` löschen + `git gc` (`.git` 182 MB) — beim Auftraggeber. Ursprünglich: Wiedervorlage Code-Review: alle blockierenden und wesentlichen Befunde aus R-1 gegen den Stand prüfen; dazu O-R (`orphan_discarded` versprochen, nie geliefert), O-S (Zustandspaar viermal gebildet), `isInside`, `useRoute`/`popstate`, `errorText.ts` | code-reviewer |
| ~~R-2a~~ | **Freigegeben** für Welle A–C unter Auflagen. Alle fünf blockierenden Befunde aus R-2 erledigt, alle S-/H-Punkte erledigt; T-094 Annahme 1 trägt (SC 1.4.1 besser erfüllt); Gattungswort nicht zurückholen (`ux_pool_name` UNIQUE NOCASE). Wesentlich, keiner blockierend: W-1 OpenAPI `:2961-2963` Add-in-Buchungsroute trägt noch den gestrichenen Satz (domain-dev); W-2 `time-entry.ts:111-112` Kommentar „Karte bleibt, wo sie ist" (domain-dev); W-3 `repo-todos.ts:567` `markDone`-Kommentar, dazu in die falsche Richtung (domain-dev); W-4 `docs/architektur.md:334-344` widerspricht `:105-111` (D-3 aus R-2; domain-dev, weil zuletzt in T-093 seine); W-5 Stopp-/Orphan-Toast ohne Todo-Namen — „Es" ohne Bezug, beim Wechsel A-6.8 zwei „Es"-Sätze für verschiedene Todos; Rahmen wie im Add-in: Titel „Zeit gebucht auf „X“." (frontend-dev); W-6 `BoardScreen.tsx:419-423` „Vom Board nehmen" schließt Dialog, „Als Spalte aufnehmen" nicht, Undo-Toast außerhalb `aria-modal` (frontend-dev); W-7 Hilfssatz zum Widerspruch „Nicht abgerechnet"(Buchung)/„Abgerechnet"(Spalte) an `RuleSummary` bei `exported` (frontend-dev). T-097 Frage 1: Absage im `ConfirmDialog` liegt in `aria-describedby`, wird nicht angesagt (SC 4.1.3) — Titel/Knopf wechseln wie `StatusSettings`, nach T-099; Frage 3: keine Ansage, `RefreshHint` auf die übrigen Ansichten. O-U-Rat: `DELETE /done` = `'reopen'`, `PUT /done` = `'booking'` mit Satz an `PoolMovementOccasion`. Auflagen: W-1–W-3 **vor** dem documenter; D-1/D-2 beschreiben, nicht zitieren. Ursprünglich: Wiedervorlage Spezifikation/UX: B-1 bis B-5, D-1/D-2 aus R-2; E-058 Punkt 4 (Satz ohne Gattungswort) an allen Flächen; E-059-Wortlaute; T-094 Annahme 1 (Exportachse in `RuleSummary` ohne `ExportStatusBadge`); Löschdialoge mit Regelnamen; Ordner-Löschdialog wechselt nach Absage Titel/Knopf nicht (T-097 offene Frage 1); Ansage für Bildschirmleser bei erneutem Laden (T-097 Frage 3) | spec-ux-reviewer |
| ~~R-3a~~ | **Freigegeben.** Sechs Nacharbeiten aus R-3 erledigt und nachgemessen (Blobs nicht mehr in HEAD-Historie, Grenzwächter mit Untergrenzen, `commaSeparatedIds` 1..50, `poolReference`, `matchesPool`-Wache); `navigate()` kann bauartbedingt nur `#/…` erzeugen; `details[]` als React-Text; `useDataFreshness` ohne Schleife/Zeitgeber; Migration 0012 mit `pragma_foreign_key_check`, 24 eingebettete Migrationen zeichengleich mit `.sql`. Flächenzuwachs nur inhaltlich: Namen reiner Board-Spalten gehen über `poolMovement` ans Add-in (E-056/E-058, Bedrohungsmodell 15.3). Sollte: S-1 Sicherungszweig hält 186 MB erreichbar (Auftraggeber); S-2 `migrations:embed:check` von keiner Kette gerufen — **erledigt vom Orchestrator: `proof:migrations` in `proof:all`**; S-3 `release.yml` hält `SHA256SUMS` nie gegen die Dateien — `sha256sum -c` vor `gh release create` (Orchestrator, auf `fix/windows-sidecar-bundle-check`). Hinweise: H-2 `input.ts:57,58` Namen mit Steuer-/Bidi-Zeichen (domain-dev); H-3 `details` ohne LIMIT (domain-dev/frontend-dev); H-4 `migration-runner.ts:274` `legacy_alter_table` nicht im `finally` (domain-dev); H-5 `router.ts:93,106`/`useRoute.ts:75` `decodeURIComponent` ohne Netz (frontend-dev); H-6 `visibilitychange` ohne Mindestabstand (frontend-dev); H-7 `tag-folder-rule-lock.spec.ts` `new RegExp` aus Namen (e2e-tester). Semgrep Guardian erneut nicht erreichbar, 42Crunch weiter nicht betriebsbereit; `tests/fixtures/**` leer — Regel oder Ort anpassen (Auftraggeber). Bedrohungsmodell Abschnitt 15. Ursprünglich: Wiedervorlage Sicherheit: Befunde aus R-3 gegen den Stand; neu: `details[]` (Regelnamen) in der Oberfläche, `poolMovement` an drei Timer-Antworten, `navigate()` über `location.assign`, `useDataFreshness` (`visibilitychange`), Migration 0012, `paths.mjs`; Bedrohungsmodell nachziehen | security-checker |

Danach documenter (D-1/D-2 Benutzerhandbuch, „Regel über Tags" in `docs/`).

### Welle vom 2026-09-04, Welle E (Befunde aus Welle D) — fertig

Stand `ece52a7` plus Orchestrator: `apps/desktop/tsconfig.test.json` (in `typecheck:test`),
`tests/e2e/tsconfig.json` mit neuem Skript `typecheck:e2e` (noch **nicht** in `typecheck`, weil
vier Typfehler offen sind — T-103), S-3 `sha256sum -c --strict` in `release.yml` auf
`fix/windows-sidecar-bundle-check` (10556ad, gepusht, gemergt). Entscheidungen E-060 (O-U) und
E-061 (O-S/O-T). Drei Agenten parallel, disjunkte Pfade; `proof:*` und `test:e2e` binden
Port 17843 — warten, keine fremden Prozesse beenden.

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-101~~ | **Fertig.** E-060: beide `/done`-Routen liefern `poolMovement` **flach neben dem Todo** (`TodoAfterDone`, additiv; nicht `{ todo, poolMovement }` — T-103-Helfer anpassen), `null` = Kennzeichen unverändert. E-061 Punkte 1/2: `BOOKING_EFFECT` und `ENTRY_CLOSED_EFFECT` in `packages/domain/src/time-entry.ts` (Stopp schreibt kein `completed_at`, deshalb zwei Wirkungen, `BOOKING_EFFECT extends EntryClosedEffect`), `bookingMovementStates`/`closedEntryMovementStates`/`completionMovementStates`/`NO_ENTRIES` exportiert in `usecases/pool-movement.ts`; Start/Stopp/`orphaned/resolve` rufen sie. O-R: eigener `ResolveOrphanedTimerResult`, `orphan_discarded` durchgereicht, OpenAPI dazu. `TagPort.remove` mit Regelnamen; H-3: 21 geholt, 20 genannt, Kürzung im Meldungstext; H-2 weist Steuer-/Bidi-Zeichen ab (Altname mit Steuerzeichen ließe sich per `PATCH` nicht unverändert zurückschicken — gemeldet); H-4 `finally`. Kommentare W-2/W-3/W-4, CASCADE-Texte. **W-1 nicht erledigt**: OpenAPI-Beschreibung der Add-in-Buchungsroute liegt nach E-053 im Add-in-Abschnitt und nennt `poolNames`, das in Welle F fällt — an integration-dev. Nachweis: `typecheck` 0, `test` 0 (684), `proof:openapi` 100/0, `proof:all` 0 (14 Pfade, 828). Kein bestehender Test rot. Welle F: integration-dev ruft `bookingMovementStates(todo, { hasOpen, hasExported })`, lokales `BOOKING_EFFECT` in `routes/addin/service.ts:246` und beide Handpaare entfallen. unit-tester: `switchTodoDone`, `closedEntryMovementStates` gegen `bookingMovementStates`, `orphan_discarded`. Ursprünglich: Dienst und Domäne: (1) E-060 — `PUT`/`DELETE /todos/{todoId}/done` liefern `poolMovement` (`'reopen'` bei DELETE, `'booking'` bei PUT), OpenAPI dazu, Kommentar an `PoolMovementOccasion` nennt beide Anlässe der neutralen Form; (2) E-061 Punkte 1 und 2 — Wirkung in `packages/domain`, `bookingMovementStates` in `usecases/pool-movement.ts`, Timer-Routen darauf umgestellt (Add-in-Routen **nicht** anfassen, Welle F); (3) O-R — `usecases/timer.ts:511` gibt `orphan_discarded` aus der Domäne durch statt `timer_too_short` zu setzen, `timer.ts:294` und OpenAPI `:2043` dazu, **unterscheiden, nicht kürzen**; (4) `repo-tags.ts:213` `TagPort.remove` liefert `details` mit Regelnamen wie Ordner und Status (T-099 hat den Ausfall im Dialog gemessen); (5) Kommentare `repo-tags.ts:192`, `repo-statuses.ts:277` (CASCADE-Begründung), W-2 `time-entry.ts:111-112`, W-3 `repo-todos.ts:567`, W-1 OpenAPI `:2961-2963`, W-4 `docs/architektur.md:334-344` gegen `:105-111`; (6) H-2 `input.ts:57,58` Steuer- und Bidi-Zeichen in Namen abweisen, H-3 `details` mit Obergrenze, H-4 `migration-runner.ts:274` `legacy_alter_table` ins `finally`. Zeilennummern aus R-1a/R-2a/R-3a, Stand `aca53df` | domain-dev |
| ~~T-102~~ | **Fertig.** Neu `apps/web/src/lib/movement.ts` (Handlung→Anlass an einer Stelle). Bewegungssatz nach „Erledigt"/„Wieder offen" an Board, Liste, Detail — gegen die echte Route gemessen (`TodoDoneResult extends Todo`, `poolMovement` daneben); verdrängt die pauschale Pool-/Board-Zeile. Stopp-/Orphan-Toast mit Todo-Namen im Titel (W-5), `ResolveOrphanedTimerResult` mit Text je Grund (O-R), W-6 Dialog schließt vor dem Undo-Toast, W-7 kurzer Satz an `RuleSummary`, `labels.ts` aus der Domäne, `verify-node-checksums.mjs` Untergrenze 6 mit Gegenprobe, `navigate()` löst Wiederbesuch selbst aus, `useAsync` ohne Platzhalter beim Nachladen (zwei Abhängigkeitslisten, 13 Aufrufstellen), `decodeURIComponent` mit Netz (H-5), Mindestabstand 1 s bei `visibilitychange` (H-6), Ordner-Dialog wechselt nach Absage Titel/Knopf („Abbrechen" → „Schließen"), `RefreshHint` überall (W-12). Nachweis: `typecheck` 0, `test` 0 (684), `contrast` 0/432, `@takt/web` build 0, `test:e2e` 40/45 — fünf rot durch Wortlaute (`timer-stop-announcement.spec.ts:103-104, :173-176, :256`, `tag-folder-rule-lock.spec.ts:152, :213`), gemessene Wortlaute in Abschnitt 2 des Berichts. Fragen: (1) „Zeit gebucht." nach Buchung von Hand (`BookingDialogs.tsx:107`) nennt das Todo anders, `POST /time-entries` liefert kein `poolMovement` obwohl es die erste Buchung sein kann → **O-V** (domain-dev, dann frontend-dev); (2) W-13 `Kanban.tsx:144`, `DESIGNSYSTEM.md:623` → Nachzieher. Ursprünglich: Oberfläche und Hülle: (1) E-060 Punkt 4 — Toast nach „Erledigt" und „Wieder offen" trägt den Bewegungssatz aus `poolMovement` (`'past'`, Zeile weg bei `null`); Vertrag: beide Routen antworten mit `poolMovement: PoolMovement \| null`; (2) O-R — `StopTimerResult` in `types.ts:614` für `orphaned/resolve` um `orphan_discarded` erweitern, Toast-Text je Grund; (3) W-5 Stopp-/Orphan-Toast mit Todo-Namen im Titel („Zeit gebucht auf „X“.") statt „Es" ohne Bezug; (4) W-6 `BoardScreen.tsx:419-423` beide Knöpfe schließen den Dialog, Undo-Toast nicht außerhalb `aria-modal`; (5) W-7 Hilfssatz an `RuleSummary` bei `exported`; (6) R-1a (4) `labels.ts:86/239` Aufzählungen aus der Domäne importieren, (5) `verify-node-checksums.mjs:91` Untergrenze sechs, (6) `router.ts:134` `navigate()` löst den Wiederbesuch bei gleichem Ziel selbst aus, (7) `useDataFreshness.ts:73` `bump()` ohne Platzhalter; (7) H-5 `router.ts:93,106`/`useRoute.ts:75` `decodeURIComponent` mit Netz, H-6 Mindestabstand bei `visibilitychange`; (8) Ordner-Löschdialog wechselt nach Absage Titel/Knopf wie `StatusSettings` (T-097 Frage 1), `RefreshHint` auf die übrigen Ansichten (Frage 3). Zeilennummern Stand `aca53df` | frontend-dev |
| ~~T-103~~ | **Fertig.** `typecheck:e2e` Exitcode 0 (drei `ChildProcessByStdio<null, Readable, Readable>`-Aliasse, `noCreateOffer` als `Promise<void>`), H-7 in `tag-folder-rule-lock.spec.ts` und `tags-folders.spec.ts` (neun Stellen, Zeichenkette statt `RegExp`), Entwurfshelfer `setTodoDoneWithMovement`/`reopenTodoWithMovement` in `support/api.ts` (Hülle `{ todo, poolMovement }` **angenommen**, gegen T-101 prüfen), `docs/testplan.md` Abschnitt 18. Entwürfe für E-060/O-R im Bericht. Zwei fremde Fehlschläge dokumentiert, Nachziehwelle: Tag-Löschdialog nennt jetzt die Regel (`tag-folder-rule-lock.spec.ts` erwartet noch das Fehlen), Stopp-/Orphan-Toast trägt den Todo-Namen im Titel (`timer-stop-announcement.spec.ts`). Frage: Toast-Titel „„X“ ist erledigt." gegen E-060 Punkt 4 — nach T-102 entscheiden. Ursprünglich: End-to-End-Pflege: (1) `pnpm run typecheck:e2e` auf 0 bringen — `support/services.ts:160`, `web-build-services.ts:170`, `global-setup-outlook-build.ts:90` (`ChildProcessByStdio<null, …>` gegen `ChildProcessWithoutNullStreams`), `tag-input.spec.ts:78` (Rückgabetyp `ReturnType<typeof expect>` statt `Promise<void>`); (2) H-7 `tag-folder-rule-lock.spec.ts` `new RegExp` aus Namen — maskieren oder ohne RegExp; (3) Testfälle für E-060 (Toast nach „Erledigt"/„Wieder offen" mit Bewegungssatz) und O-R (`orphan_discarded`) **vorbereiten**, laufen erst, wenn T-101/T-102 stehen — als `test.skip` mit Verweis nicht einchecken, sondern im Bericht als Entwurf; (4) `docs/testplan.md` | e2e-tester |

Danach: integration-dev (E-061 Punkt 3, Add-in-Routen auf `poolMovement`; Welle F), unit-tester
und e2e-tester Nachzieher, Reviewer-Wiedervorlage, zuletzt documenter.

### Welle vom 2026-09-04, Welle N (die letzten Doppelungen, drei Fälle aus T-124) — fertig

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-128~~ | **Fertig.** **O-AL:** `MAX_TITLE_CHARACTERS` in `packages/domain/src/text-length.ts`; beim Nachsehen fielen zwei weitere Zahlen derselben Klasse an — `MAX_NAME_LENGTH` trug den Kommentar „dieselbe Zahl wie `nameSchema` im Dienst", **ohne dass es jemand erzwang**, und `MAX_PAGE_SIZE` gegen `paginationSchema`; beide lesen jetzt, statt zu tragen. **O-AP:** `proof-codepoints.mjs` (45 Prüfungen) läuft über versionierte **und neu angelegte** Dateien, **leitet seine Klasse aus `FORBIDDEN_NAME_CHARACTERS` ab statt sie abzuschreiben**, prüft Binärdateien darauf, dass sie welche **sind**, und hat eine Ausnahmeliste, in der jede Ausnahme Pfad, Codepunkt, **Anzahl** und Grund nennt und rot wird, sobald sie nicht mehr genau zutrifft — heute leer. **O-AQ:** `headersTimeout` 5 s, dazu `requestTimeout` 10 s und `connectionsCheckingInterval` 5 s, weil die beiden anderen die Angabe sonst wertlos machten (`proof:access` Abschnitt 0f, +5). Die vier defensiven Zweige aus T-127 sind zu einem `codePointOf` zusammengelegt. **Der Wächter hat als Erstes seinen eigenen Autor erwischt:** drei rohe `U+0000` in `proof-codepoints.mjs` selbst, hineingeraten unmittelbar nach dem Lesen des Trenners aus `paging.ts` — behoben mit `JSON.stringify`. Damit **sieben Vorfälle in fünf Aufgaben**, und dieser ist der erste, den eine **Messung** fand, bevor er in einen Commit kam. Nachweis: `typecheck` 0, `test` 1001/1001, `proof:all` **891**, `proof:access` **97**, `proof:codepoints` 45, `boundaries` 0. **Orchestrator erledigt:** `proof:codepoints` als **erstes** Glied in `proof:all`. **Offen:** die drei 4000er (zwei bei integration-dev, eine davon eine Doppelung *innerhalb* von `routes/addin/schema.ts`), `.gitattributes` fehlt, Fristen für den Aufgabenbereich auf 17844, `tagIds: max(200)` an zwei Türen, Homoglyphen nicht erfasst — als O-AR. Ursprünglich: **O-AL:** `MAX_TITLE_CHARACTERS = 500` ist die letzte doppelte Wahrheit im Add-in — Zahl nach `packages/domain`, `titleSchema` liest sie; das **Add-in** zieht später nach (integration-dev), du bereitest nur vor. **O-AP:** Wächter, der rohe Steuerzeichen dauerhaft aus dem versionierten Produktivcode hält — vier Agenten sind in vier Wellen über dasselbe Zeichen gestolpert, zuletzt der, der es beheben sollte, dreimal in einer Aufgabe; Skript in `apps/local-api/scripts`, Eintrag in `proof:*` durch den Orchestrator. **O-AQ:** `headersTimeout` im Betrieb heruntersetzen oder begründet hinnehmen. Dazu die fünf defensiven Totzweige aus T-127 (`characters.ts:221,242,264,297`, `enumeration.ts:53`) bewerten | domain-dev |
| ~~T-129~~ | **Fertig, und weiter gegangen als beauftragt.** Die **Herkunft steht jetzt im Typ** statt in einer Liste im Nachweis: `api/types.ts` führt `ForeignText`, `DraftText`, `ServiceText`, `TechnicalKey`, `FileSystemPath`, `ColorValue`, `PageCursor`, `SecretText` — **kein Feld dort heißt mehr bloß `string`** (55 klassifiziert). `proof-foreign.mjs` (789 Zeilen, 10 Prüfungen) fragt den Übersetzer nach der Marke und **kennt keinen einzigen Feld- oder Funktionsnamen**; auch die Behandlungen erkennt er an ihrer Signatur. Der Gegenentwurf — eine gepflegte Namensliste — wäre genau die Bauart, die E-063 Punkt 4 verbietet. **Der Fund:** elf rohe Anzeigestellen und sieben rohe Sätze, die T-124 an 175 behandelten Stellen vorbei übersehen hatte — darunter Begründung und Auslöser **jeder Exportzurücksetzung** und der Wert **jeder Zelle der Exportvorschau**, also die Ansicht, an der ein Benutzer prüft, was er abrechnet. Alle 18 behoben statt nur gemeldet („ein Wächter, der beim ersten Lauf rot ist, wird abgeschaltet"). Call-Nummern gelten jetzt als fremd — entgegen der eigenen T-124-Ausnahme, weil der Zeichenvorrat an der **heutigen** Tür geschlossen ist, nicht im Bestand. **Blinde Flecken vollständig benannt:** fertige Sätze des Dienstes (`ApiError.message`) und der Domäne (`poolMovementSentence`), die `unknown`-Grenze an `ExportTemplate.definition`, absichtliche Umgehungen, und der Bildschirm selbst (das misst E-062). Nachweis: `typecheck` 0, Web-Tests 71/71, build 0, `contrast` 0/432, `proof:foreign` 10/0; **sieben Gegenproben** je Endstatus 1, danach `sha256sum -c` über alle berührten Dateien OK. **Orchestrator erledigt:** `proof:foreign` in beide `package.json` und vor `proof:addin` in `proof:all`; **E-063 Punkt 6** aufgenommen. Ursprünglich: **O-AM:** statischer Nachweis für `apps/web`, „kein roher fremder Wert im JSX", nach dem Vorbild `proof-addin.mjs` Abschnitt 17 — die Behandlung am Anzeigebaustein ist an der Aufrufstelle unsichtbar (T-124 R3, E-063 Punkt 5). Skript unter `apps/web/scripts`; den `proof:*`-Eintrag macht der Orchestrator | frontend-dev |
| ~~T-130~~ | **Fertig, `test:e2e` 62/62** (Marke 58/58), zweifach reproduziert. Neu `shell-shim.ts` (konfigurierbare Hüllen-Nachbildung mit echtem Sitzungsnachweis; sonstige Anfragen laufen unverändert gegen den echten Dienst), `db.ts`, `bidi.ts`. **O-AF:** `takt_quit`, das nie auflöst, plus `page.clock.fastForward('00:06')` — `.quitfail` mit zwei Schritten, kein „Systembetreuung". **O-AJ:** erfundener Name mit `U+200F`, einmal **mit** und einmal **ohne** `service_exit`, gegen `alertdialog`, Tabulatorfalle und „kein Element trägt den Namen". **O-AH:** Titel `Rechnung<U+202E>gnp.exe` direkt in die SQLite-Testdatenbank gelegt, **weil die Tür ihn mit 422 abweist** — und mit eigenem Rangetest geprüft, dass der Text im `<bdi>` **tatsächlich** links-nach-rechts erscheint, nicht bloß dass ein `<bdi>` da ist. Testplan Abschnitt 23: TP-SHELL-01, TP-SHELL-02, TP-BIDI-01. **Zwei Beobachtungen, gemeldet statt gedeutet:** ein transienter Fehlschlag in `toast-tab-order-scroll.spec.ts` bei `--retries=0` (fremde Datei aus T-120, isoliert sofort grün, mit Vorgabewert zweifach 62/62) und derselbe Parse-Fehler `characters.ts:187` wie bei T-131, verursacht durch ein rohes `U+202E` aus der parallelen T-128-Arbeit. Ursprünglich: Die drei Fälle aus T-124, die alle ein Ereignis oder eine Zeit brauchen (E-062): **O-AF** `page.clock.fastForward('00:06')`, `.quitfail` steht, zwei Schritte, das Wort „Systembetreuung" kommt **nicht** vor; **O-AJ** Start mit `U+200F` im Namen — `alertdialog` steht, Tabulator bleibt darin, kein Element trägt den Namen; **O-AH** Titel `Rechnung<U+202E>gnp.exe` aus der Datenbank erscheint in der Liste als `Rechnung<U+FFFD>gnp.exe` in einem `<bdi>` | e2e-tester |
| ~~T-131~~ | **Fertig.** Die Randtabelle liegt nur noch in `packages/domain/test/characters.test.ts`. `input.test.ts` leitet seine Randwerte **zur Laufzeit** aus `FORBIDDEN_NAME_CHARACTERS`/`CONTROL_WHITESPACE` ab und prüft damit die zod-Bindung — dass `titleSchema`/`nameSchema` die Klasse **anwenden** —, nicht die Klasse selbst (31 → 59 Tests). `hidden.test.ts` prüft Gleichheit der Objekte (`toBe`, nicht `toEqual`) statt eines zweiten Verhaltensvergleichs (23 → 5). **Zwei Gegenproben beziffern den Gewinn:** die **echte historische** Add-in-Fassung aus `55ed731` bleibt bei 6/6 alten Verhaltensprüfungen grün, aber 3/3 neue Gleichheitsprüfungen werden rot; eine simulierte künftige Domänen-Erweiterung lässt 24/24 fest getippte Randwerte grün, während die abgeleitete Fassung den neuen Fall sofort findet. **Beide Gegenproben liefen außerhalb des Arbeitsbaums** (`/tmp/.../t131/`), weil Produktivcode nicht seine Hoheit ist — auch nicht vorübergehend; vorbildlich. Nachweis: `typecheck:test` 0, `test` **1001** (58 Dateien), Abdeckung 91,37/84,9/94,3/93,95 %, drei Schwellen gehalten. **Gemeldet, nicht gedeutet:** einmaliger Parse-Fehler `characters.ts:187` durch paralleles Schreiben von T-128, folgenlos. Ursprünglich: T-121 Frage 3, jetzt frei: Die Randwerte der Zeichenklasse stehen an **drei** Stellen unabhängig geschrieben (Tür, Add-in, Domäne direkt) — durch eine echte Gleichheitsprüfung gegen `@takt/domain` ersetzen, nach dem Muster von T-123 (Gleichheit der Sache, nicht der Ergebnisse; E-063 Punkt 5) | unit-tester |

Nicht in dieser Welle: **O-AN** (`poolMovementSentence` trägt Poolnamen ungeschützt) — die Behebung ändert vierzehn zeichengenau geprüfte Sätze und kollidiert damit gleichzeitig mit e2e-tester und unit-tester; eigene Welle. Danach Welle O: Reviewer über Welle J bis N, **zuletzt der Dokumentierer**.

### Welle vom 2026-09-04, Welle M (Befunde aus T-125, Abdeckung der geteilten Fassung) — fertig

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-126~~ | **Fertig.** **T-125-4 behoben, aber nicht wie vorgeschlagen:** `closeAllConnections()` ist das Mittel, die Frist (`SHUTDOWN_DEADLINE_MS = 2_000`) nur der Boden darunter — eine Schonfrist für laufende Anfragen wäre reiner Verlust, weil `database?.close()` zwei Zeilen vorher läuft. `proof:access` Abschnitt 0e misst nicht nur *ob*, sondern *wann*: ohne Behebung lief der Dienst nach **20 006 ms** noch, mit ihr endet er nach **8 ms**; zweite Gegenprobe mit stillgelegtem `closeAllConnections` zeigt 2010 ms und macht genau die sechste Prüfung rot. **Behoben ist damit:** Jeder Prozess auf dem Rechner konnte das Ende von Takt um bis zu fünf Minuten verzögern, ohne ein Geheimnis zu kennen. **T-125-6:** `SEPARATOR` in `paging.ts` als Escape-Folge, Datei gemessen wieder Text für Git (84 Zeilen statt „Bin"); Scan über **alle** versionierten Dateien findet keine weitere Stelle — **T-125-H7 damit auch zu**. **Startreihenfolge:** richtig, und der Grund steht jetzt an der Stelle — seit T-122 geht das Dateiende nicht mehr verloren, es wird nur später zugestellt, nämlich wenn der Dienst fertig gebaut ist, und genau das erspart ein `shutdown()` mit halbem Bestand. Nachweis: `typecheck` 0, `test` **991/991**, `proof:access` **92** (vorher 86), `proof:all` **886**, `boundaries` 0. **Für den Code-Reviewer:** `paging.ts` als Ganzes lesen, nicht über den Diff — `git diff` zeigt sie bis zum Commit weiter als „Bin". Das rohe Nullzeichen ist dem Agenten bei dieser Aufgabe **dreimal** selbst in die Arbeit gerutscht (zweimal vom Werkzeug abgewiesen, einmal erst vom eigenen Scan gefunden) — dritter Beleg nach T-119 und T-125. Ursprünglich: **T-125-4:** `apps/local-api/src/main.ts:275-281` `shutdown()` ohne Frist — `server.close()` wartet auf offene Verbindungen; auf Node v22.23.2 gemessen: ein lokaler Prozess mit halbem Anfragekopf verhindert den Rückruf über 8 s hinaus (Schranke 60/300 s). Gegenmittel `closeAllConnections()` + `setTimeout(…).unref()`, Nachweis `proof:access` Abschnitt 0e mit offener Verbindung. **T-125-6:** `packages/storage/src/sqlite/paging.ts:40` rohes `U+0000` macht eine Produktivdatei für Git binär — seit dem ersten Commit nie im Review lesbar; als Escape-Folge schreiben. Dazu prüfen, ob `server.listen` (`main.ts:217`) vor `watchParentLink` (`:283`) die richtige Reihenfolge ist (T-125, Abschnitt 17) | domain-dev |
| ~~T-127~~ | **Fertig.** Neu `packages/domain/test/characters.test.ts` (25 Tests, 137 Fälle) misst alle drei Behandlungen aus E-063 **an der Quelle** gegen eine 27-Werte-Randtabelle (C0/C1/DEL, Bidi-Einbettungen und -Isolate, die drei Marken, ihre erlaubten Nachbarn samt Familien-Emoji über ZWJ), und `enumeration.test.ts` (17). **Die Prozentzahlen bewegten sich nicht** (97,22/96/100/100 % bzw. 100/83,33/100/100 % vorher wie nachher) — die Ränder waren über die vier T-122-Leser bereits mittelbar gedeckt; der Wert liegt darin, die Klasse an der Quelle zu messen statt durch ihre Leser hindurch. Das ist die ehrliche Auskunft zu meiner Annahme, 48 % seien eine Lücke. **T-125-H7:** zwei gemeldete Berichte plus alle zwölf eigenen gescannt, dabei zwei **nicht gemeldete** rohe `U+FFFD` gefunden; beim ersten Entwurf des eigenen Berichts sind 18 rohe Zeichen unterlaufen, obwohl die Absicht von Anfang an Escape-Folgen waren — ein Codepunkt-Scan vor Abgabe hat sie gefangen. **Vierter Beleg für O-AP.** Nachweis: `typecheck:test` 0, `test` **991/991** (58 Dateien), Abdeckung 91,32/84,91/94,29/93,95 % unverändert, zwei isolierte Rot-vor-Grün-Nachweise mit `diff`-Bytegleichheit danach, Codepunkt-Scan über beide neuen Dateien und alle 13 Berichte: 0 Treffer. **Hinweis an domain-dev:** fünf defensive Totzweige (`characters.ts:221,242,264,297`, `enumeration.ts:53`) — gemeldet statt künstlich kaschiert. **Offen (Welle N):** T-121 Frage 3 ist jetzt frei, weil T-123 fertig ist: Die Randwerte stehen an **drei** Stellen unabhängig geschrieben (Tür, Add-in, Domäne) — durch eine echte Gleichheitsprüfung ersetzen (E-063 Punkt 5). Ursprünglich: T-121 Frage 2: `packages/domain/src/characters.ts` (48 % Zeilenabdeckung) und `enumeration.ts` abdecken — die Klasse ist seit T-122 die **eine** Wahrheit, ihre Ränder gehören gemessen. **T-125-H7:** rohe unsichtbare Zeichen in `.claude/team/reports/T-111-unit-tester.md` und `T-121-unit-tester.md` als Escape-Folgen; sie verbreiten sich beim Zitieren weiter, und niemand sieht dabei etwas | unit-tester |

Noch nicht startbar (Kollision mit der laufenden Welle L): T-121 Frage 3 (doppelte Randmessung durch Gleichheitsprüfung ersetzen) hängt an T-123, der die Nachweisabschnitte gerade umbaut; die Reviewer und der e2e-Lauf kommen nach T-123/T-124. **Zuletzt der Dokumentierer.**

### Welle vom 2026-09-04, Welle L (auf die geteilte Fassung nachziehen, Sicherheit) — fertig; **security-checker gibt Welle J bis L frei**

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-123~~ | **Fertig.** `hidden.ts` führt die Klasse nicht mehr, sondern reicht `dropHiddenCharacters`, `hasHiddenCharacter`, `visibleText` und `HIDDEN_MARKER` aus `packages/domain/src/characters.ts` durch; die Anzeige markiert weiter (E-063 Punkt 2). `cut.ts` unberührt — seine Grenze ist die Definition von UTF-16, keine Regel von Takt. **Die beiden Nachweisabschnitte fragen jetzt, ob es zwei Fassungen *gibt*, statt ob zwei Fassungen dasselbe *tun*:** Gleichheit der Objekte (`dropHidden === dropHiddenCharacters`), kein zweiter Träger im Quelltext, keine zweite Aufzählung in einer Beschreibung. Behalten: der BMP-Scan gegen jede Tür (misst die zod-Bindung, nicht die Klasse) und der Titelvorschlag über die ganze Klasse. T-117 R1: Die Beschreibungen zählen nicht mehr auf, sie verweisen — und **dass** sie es nicht tun, wird gemessen. `istLeerraum` liest ungefragt mit (zwei Zeilen, vom security-checker in 17.9 benannt). **Gegenprobe A, die wichtigste Messung dieser Welle:** Mit der alten, **zeichengleichen** Fassung von `hidden.ts` bleiben **163 Prüfungen grün**, nur die zwei neuen fallen aus — ein Ergebnisvergleich wird erst rot, wenn die Doppelung schon falsch ist. Als **E-063 Punkt 5** aufgenommen. Nachweis: `typecheck` 0, `typecheck:test` 0, `test` 837/837, `proof:addin` **165** (vorher 164), `proof:all` 0 (**880**), `boundaries` 0, Add-in-Build 0 (238,68 kB, +0,41 kB durch Codepunktschleifen). Drei Gegenproben, jede danach über `sha256sum -c` bytegleich zurückgesetzt. **Offen:** `MAX_TITLE_CHARACTERS = 500` ist die letzte doppelte Wahrheit im Add-in (O-AL, domain-dev); `docs/bedrohungsmodell.md` 17.9 nennt „proof:addin 17" für den BMP-Scan, er liegt jetzt in 16 (security-checker). T-125-1 bis -3 eingelöst. Ursprünglich: O-AK: `apps/outlook-addin/src/text/hidden.ts` liest `packages/domain/src/characters.ts` statt eigener Fassung; die zwei Nachweise in `proof-addin.mjs` (Abschnitte 16/17) prüfen danach **Gleichheit der Quelle** statt Zeichen gegen Zeichen (E-063 Punkt 4); T-117 R1: `routes/addin/schema.ts:88-93` und OpenAPI Add-in-Abschnitt `:2905`/`:2988` beschreiben die Klasse unvollständig | integration-dev |
| ~~T-124~~ | **Fertig, vier Punkte.** **O-AK:** Die drei Aufzählungsabschriften sind fort; `errorText`, `poolRule` und `TodoFormDialog` rufen `enumerateGerman`/`quoteName`/`enumerateNames` aus `@takt/domain`, der T-110-Wortlaut steht zeichengleich (71 Web-Tests). **O-AJ:** Die Oberfläche fragt beim Verbinden einmal `hasForbiddenNameCharacter` über den Windows-Namen und behält nur die Antwort; bei `forbidden_characters` tritt eine eigene Sperrmeldung **vor** die Meldung zum Dienstausfall, nennt zwei Wege ohne fremde Hilfe, sagt wo die Daten liegen, und die Systembetreuung bekommt einen eigenen Satz (F-15) — deckt **beide** Startwege ab, auch den, bei dem die Hülle abbricht und gar kein `serviceExit` entsteht. **O-AF:** Frist statt `catch`, weil der Erfolgsfall dieses Knopfes der Tod des eigenen Prozesses ist — nach fünf Sekunden steht die Auskunft im Feld, mit „Fenster schließen" und Task-Manager statt „Systembetreuung". **O-AH:** neuer Baustein `Foreign` (45 Stellen), `quotedName` (103), `foreignText` (27); Eingabefelder unangetastet (E-063 Punkt 1), Vorschläge über `dropHiddenCharacters`. **`apps/desktop` bewusst unverändert**, obwohl eigene Hoheit: `handshake_line` prüft mit `c.is_control()` und kennt die Richtungszeichen nicht, `explain_exit(78)` beschreibt `user_invalid` falsch — beides zu beheben hieße, die Zeichenklasse ein **drittes** Mal in den Baum zu schreiben, diesmal in Rust, genau gegen E-063 Punkt 4; die Auskunft hängt nicht daran, sie befragt den Namen, nicht den Exitcode. Nachweis: `typecheck` 0, Web-Tests 71/71, web build 0, `build:designsystem` 0, `contrast` 0/432; Browsermessung: „Rechnung\<U+202E\>gnp.exe" steht roh **nicht** in Leserichtung, durch `Foreign` schon; Sperrmeldung mit Titel, `aria-labelledby`/`-describedby`, Fokus, zwei Schritten und leerer Live-Region. **R3, ehrlichste Lücke:** Eine Behandlung am Baustein ist an der Aufrufstelle unsichtbar, und für `apps/web` gibt es keinen statischen Nachweis wie `proof-addin.mjs` Abschnitt 17 → **entschieden: wird gebaut** (O-AM). **An e2e-tester (Welle N):** drei Fälle, die alle ein Ereignis oder eine Zeit brauchen (E-062) — O-AF `page.clock.fastForward('00:06')`, O-AJ Start mit `U+200F` im Namen, O-AH Titel aus der Datenbank. Ursprünglich: O-AK: die drei Aufzählungsfassungen in `apps/web` auf `enumerateNames`/`enumerateGerman`/`quoteName` aus `@takt/domain` (B-11); **O-AJ:** die Hülle sagt, warum der Start scheitert (`user_invalid` aus T-122) — ein Benutzer kann seinen Windows-Namen nicht ändern; **O-AF:** „Takt beenden" scheitert stumm (`App.tsx:250`); **O-AH:** die Todo-Notiz aus dem Add-in wird in `apps/web` nicht isoliert (E-063 eine Fläche weiter) | frontend-dev |
| ~~T-125~~ | **Freigegeben (Welle J bis L).** Bedrohungsmodell: neuer Abschnitt 17.0–17.9, Nachträge in 15 und zweimal in 16.4 (+383 Zeilen), die alten Stellen als Nachtrag richtiggestellt statt überschrieben — sie sind Protokoll, und die Spur, wann die Klasse gewachsen ist, ist der Gegenstand. **Antwort auf die Kernfrage:** Die geteilte Fassung trägt für den **ausführbaren** Teil (vier Stellen lesen `characters.ts`, zwei Wächter fragen die Tür). Für den **nicht ausführbaren** trug sie an `c96a2b2` nicht: Die Klasse stand in drei Prosafassungen der OpenAPI, der Wächter las genau eine — und die beiden ungewachten waren die, die T-119 nach der Regression von Hand nachtragen musste; dazu sicherte die Beschreibung zu, das Add-in lese die gemeinsame Fassung, während `hidden.ts` keine einzige `import`-Zeile hatte. **Warum die Regression überdauerte:** nicht weil niemand hinsah, sondern weil an jeder Stelle *etwas* hinsah — Nachweisabschnitt grün, Kommentar „dasselbe Schema", Beschreibung nennt die Klasse. Drei Zeugen, alle aus derselben Abschrift. Der Eindruck von Deckung wächst mit ihrer Zahl, die Deckung nicht. Bedingung ist kein Vorsatz, sondern ein Zustand: **jeder Träger liest oder wird gemessen.** **Windows-Benutzername:** Entscheidung richtig, aber aus einem anderen Grund als genannt — der Wert kommt aus `GetUserNameW`, ein solcher Name kann von dort nicht kommen; `user_invalid` ist kein Namensprüfer, sondern ein **Manipulationssignal**, und das beantwortet man nicht mit Weiterlaufen. **Sidecar:** `server.listen` bei `main.ts:217`, `watchParentLink` bei `:283` — der Dienst hört auf `127.0.0.1`, **bevor** der Wächter angemeldet ist; ein Dienst ohne Fenster hält Port, Datenbestand und Sitzungsgeheimnis. T-125-1 und T-125-2 während der Prüfung von integration-dev/frontend-dev geschlossen und nachgemessen. **Offen (Welle M):** T-125-4 `main.ts:275-281` `shutdown()` ohne Frist — auf Node v22.23.2 gemessen: ein lokaler Prozess mit halbem Anfragekopf verhindert den Rückruf über 8 s hinaus (Schranke 60/300 s); Port und Datenbank sind dann zwar frei, aber B-1.6 Punkt 3 gilt nur mit Fußnote. Gegenmittel `closeAllConnections()` + `setTimeout(…).unref()`, Nachweis `proof:access` 0e (domain-dev). T-125-6 `packages/storage/src/sqlite/paging.ts:40` rohes `U+0000` macht eine **Produktivdatei** für Git binär — seit dem ersten Commit nie im Review lesbar (domain-dev). T-125-H7 rohe unsichtbare Zeichen in `T-111-` und `T-121-unit-tester.md` (unit-tester). T-125-5 `sidecar.rs:318-327` ein Text für Code 78, für `user_invalid` falsch — T-124 hat den Kern von der anderen Seite gelöst (spec-ux-reviewer). Semgrep Guardian zum **siebten** Mal nicht erreichbar, 42Crunch weiter ohne Werkzeug. Ursprünglich: Freigabe Welle J bis L: E-063 und die T-119-Regression — **wie konnte sie fünf Wellen unbemerkt bleiben**, und trägt die geteilte Fassung aus T-122 die Antwort? Dazu die zwei T-122-Befunde (verwaister Sidecar hielt den Port, B-1.6 Punkt 3 im Startfenster offen; Anhalten mit Code 1), `WindowsUser` gegen die Klasse, Bedrohungsmodell `:2723`/`:2960`/`:3002` und die Add-in-Anzeige nach E-063 | security-checker |

Danach Welle M: unit-tester ersetzt die doppelte Randmessung durch eine Gleichheitsprüfung und deckt `characters.ts`/`enumeration.ts` ab (T-121 Fragen 2 und 3); Reviewer über Welle J bis L; **zuletzt der Dokumentierer**.

### Welle vom 2026-09-04, Welle K (Tests nachziehen, letzte Zeichenlücke) — fertig; `test` 837/837, `proof:all` 879, `test:e2e` 58/58

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-120~~ | **Fertig, `test:e2e` 58/58** (Marke 56, davon einer rot). `page.clock.install()`/`pauseAt(...)` friert die Zeit vor `gotoBoard` ein, damit ausschließlich `evict()` gemessen wird — Vorschlag 1 aus T-118, weil er die feste Stapel-Reihenfolge erhält; `pauseAt` mit 2 s Abstand, sonst reproduzierbar „Cannot fast-forward to the past". **Zwei unabhängige Funde beim Umbau:** B-7 aus T-118 hat die alte Fixture entwertet (das Board-„Als erledigt markieren" trägt jetzt selbst einen Rückweg), und eine angehaltene Uhr verträgt sich nicht mit wiederholten Ark-UI-Menü-Popovers — deren Schließ-Aufräumen hängt an einem nie feuernden `setTimeout`. Beides behoben (Todo-Liste mit Kontrollkästchen statt zweiter Board-Spalte) und im Dateikopf festgehalten. Neu `timer-switch-scrim-toast.spec.ts` (A-6.8/B-1, `MutationObserver` + `page.exposeFunction`: kein Bild mit `.scrim` und `.toast` zugleich) und `toast-tab-order-scroll.spec.ts` (SC 2.4.7, echte Tabulatortasten und `boundingBox()` auf der ältesten von sieben). Kommentar `outlook-addin-build.spec.ts:113` berichtigt. Testplan Abschnitt 22: TP-TOAST-01-Nachtrag, TP-TIMER-12, TP-TOAST-02. **Risiko benannt:** A-6.8 nicht gegen den alten, fehlerhaften Code gegengeprüft (fremde Hoheit) — die Schlussfolgerung stützt sich auf die T-118-Messung plus die `MutationObserver`-Mechanik. Ursprünglich: `toast-eviction.spec.ts:84` `toHaveCount(4)` **und** die Schleife „Die drei jüngeren stehen noch" auf die neue Uhr (`page.clock.install()` vor `gotoBoard`, `pauseAt(...)`); zwei neue Fälle: A-6.8 (kein Bild mit `.scrim` und `.toast` zugleich) und SC 2.4.7 (siebenmal „Erledigt", Tab bis zur ältesten Meldung, `boundingBox().y >= 0`); Kommentar `outlook-addin-build.spec.ts:113` (`<span class="mono">` → `<bdi class="mono">`); Testplan nachziehen | e2e-tester |
| ~~T-121~~ | **Fertig.** 50 neue Fälle, 787 → 837, Abdeckung 90,6/83,92/93,78/93,1 % (Anweisungen/Zweige/Funktionen/Zeilen), alle drei Schwellen gehalten, `typecheck:test` 0. Ränder der um ALM/LRM/RLM gewachsenen Klasse gemessen samt ZWJ-Familien-Emoji, das die Grenze nach unten festnagelt; neu `apps/web/test/app/undoDone.test.ts` (8, deckt den vorher fehlenden `catch` aus B-6 und die Verwechslung `'reopen'`/`'booking'`), `apps/outlook-addin/test/text/cut.test.ts` (10, weist den T-119-Kernbefund nach: halbierte Ersatzstelle übersteht `fromBase64(toBase64(t))` nicht, an der Zeichengrenze gekürzt schon), `hidden.test.ts` (23, gleiche Ränder wie die Tür, markiert statt streicht, rechtsläufige Schrift unangetastet). Alle vier Dateien rot vorgefahren. **Orchestrator erledigt (Frage 1):** `apps/outlook-addin/tsconfig.test.json` angelegt und in `typecheck:test` gehängt — die neuen Tests liefen unter `pnpm test` grün, aber der offizielle Typprüfpfad kannte das Paket gar nicht. Ohne `rootDir`, weil `cut.test.ts` gegen `packages/export/src/base64.ts` misst: dieselbe Funktion, die die Abrechnungsdatei schreibt — eine Abschrift wäre genau der Fehler aus E-063 Punkt 4. **Frage 2** (`characters.ts`/`enumeration.ts` aus T-122) und **Frage 3** (Randmessung durch echte Gleichheitsprüfung ersetzen, sobald die Klasse geteilt ist — die Lehre aus E-063) gehen in Welle L. Ursprünglich: T-117 R1: `test/http/input.test.ts:1-24`, `:124-143` um die Ränder der drei neuen Marken (`U+061C`, `U+200E`, `U+200F` abgewiesen; `U+061B`/`U+061D`, `U+200B`–`U+200D`, `U+2010`, ZWJ-Familien-Emoji angenommen); Tests für `undoDone.ts`, `Foreign`/`hidden.ts`/`cut.ts` (reine Funktionen — nach E-062 kein jsdom, Bausteine bleiben dem e2e-tester) | unit-tester |
| ~~T-122~~ | **Fertig.** Die Zeichenklasse liegt einmal im Baum: `packages/domain/src/characters.ts`, als **Codepunktbereiche** statt als regulärem Ausdruck — kein `lastIndex`, keine rohen Zeichen, von einem Nachweis lesbar —, mit den drei Behandlungen aus E-063 daneben (abweisen, fallen lassen, sichtbar machen). Dienst (`http/input.ts`, `access/session-secret.ts`) und `proof-openapi.mjs` lesen sie; über die ganze BMP gemessen ist sie zeichengleich mit beiden Vorfassungen (77 abgewiesene Zeichen, null Abweichungen) — **keine** Verhaltensänderung an Namen und Titeln. **O-AE:** Der Windows-Benutzername wird gegen dieselbe Klasse geprüft, eigener Grund `user_invalid`, elf neue Prüfungen in `proof:access` samt Gegenprobe. **O-AG/B-11:** `enumerateNames`/`enumerateGerman`/`quoteName` in `packages/domain/src/enumeration.ts`; das private `listPools` entfällt, die drei Abschriften in `apps/web` können nachziehen. **Zwei Befunde beim Messen, beide älter als die Aufgabe, beide behoben:** (1) Ein verwaister Sidecar hielt nach einem Lauf den Port — `readStartupHandshake` ließ `stdin` fließen, das Dateiende ging im Startfenster an einen Strom ohne Zuhörer, `watchParentLink` meldete sich danach an einem beendeten Strom an; **B-1.6 Punkt 3 war für die Dauer des Starts offen**, mit dem echten Dienst nachgestellt (15 s und weiter laufend), `proof:access` Abschnitt 0d hält es fest, Gegenprobe rot. (2) Das ordentliche Anhalten endete gelegentlich mit Code 1, weil `end` und `close` beide `onLost` auslösten und das zweite `database.close()` mit `ERR_INVALID_STATE` warf. Nachweis: `typecheck` 0, `test` 837/837, `proof:access` **86** (vorher 75), `proof:openapi` **110** (vorher 105), `proof:all` 0 (13 Ketten, 879), `boundaries` 0. **Orchestrator entscheidet (Frage):** Es bleibt beim **Abweisen** des Windows-Benutzernamens — Bereinigen hieße unter einem erfundenen Namen abrechnen, Markieren hieße `U+FFFD` in der Abrechnungsdatei. Aber die Hülle muss sagen, warum der Start scheitert: als O-AJ an frontend-dev. Ursprünglich: **O-AE:** `access/session-secret.ts:85` prüft den Windows-Benutzernamen nur gegen C0 und DEL — kein C1, keine Richtungszeichen, und der Name geht als `WindowsUser` **unverändert in die Exportdatei**; Klasse aus `http/input.ts` übernehmen, Prüffall in `proof:access`, Verhaltensänderung am stdin-Handschlag benennen. **O-AI:** Zeichenklasse **einmal** in `packages/domain`, Dienst und Add-in lesen sie dort — nach der T-119-Regression die tragende Lehre. **O-AG/B-11:** `listPools` aus `@takt/domain` ausführen | domain-dev |

Danach Welle L: security-checker (E-063, T-119-Regression und wie sie fünf Wellen unbemerkt blieb, Bedrohungsmodell `:2723`/`:2960`/`:3002`), integration-dev (T-117-R1-Beschreibungen in `routes/addin/schema.ts:88-93`, OpenAPI `:2905`/`:2988`), Reviewer für Welle J/K; **zuletzt der Dokumentierer** — beide Auflagen sind erfüllt.

### Welle vom 2026-09-04, Welle J (Auflagen der Reviewer, dann der Dokumentierer) — fertig; **beide Auflagen vor dem Dokumentierer erfüllt**

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-117~~ | **Fertig, Auflage 2 erfüllt.** B-9 ist **nicht** durch „sieben → acht" geschlossen, sondern durch das Streichen der Zahl: OpenAPI und Architekturbeschreibung nennen jetzt das Kriterium („jeder Vorgang, der eine der fünf Achsen einer Regel umlegt — der vollziehende wie der ankündigende"), die Tabelle am Bauteil `PoolMovement` ist die einzige vollständige Aufzählung; eine dritte Fundstelle derselben Klasse (`architektur.md:933`) ist mitbehoben. Die `PATCH`-Begründung zeigt auf O-X und nennt den `todoId`-Wechsel samt zweier gegenläufiger Todos, dazu erstmals eine Zeile an der Route selbst. `markTodoDone` trägt jetzt den Block des Schreibpfads: der Status bleibt, die Spalte nicht. Zeichenwache erfasst `U+061C`, `U+200E`, `U+200F` zusätzlich — `U+200B`–`U+200D` **bewusst nicht** (ZWJ hält Emoji zusammen); Ränder gemessen: `U+061B`/`U+061D`, `U+200B`–`U+200D`, `U+2010`, ZWJ-Familien-Emoji bleiben angenommen. O-AB ausgesprochen statt gebaut (Route und `movementOfStart`). **O-AD entschieden: `Todo.title` bleibt 512**, Beschreibung nachgezogen statt Zahl gesenkt — die Datenbank kennt keine Längengrenze (`0001_initial.up.sql:75-82`), 512er-Altbestand existiert, und 500 an einer **Antwort** wäre eine Zusicherung über fremde Bestände. Nachweis: `typecheck` 0, `test` 787/787, `proof:openapi` 105/0, `proof:all` 0 (13 Ketten, 847), `boundaries` 0. **Folgeaufgaben Welle K (R1, Klasse unvollständig beschrieben):** `test/http/input.test.ts:1-24, :124-143` (unit-tester), `proof-addin.mjs:3352-3374`, `routes/addin/schema.ts:88-93`, OpenAPI `:2905`/`:2988` (integration-dev), `docs/bedrohungsmodell.md:2723`/`:2960`/`:3002` (security-checker). Ursprünglich: **Auflage 2 (B-9).** `docs/architektur.md:127-128` „drei Vorgänge" und OpenAPI `:4195` „sieben" gegen acht Zeilen der eigenen Tabelle (`:4199-4208`); `architektur.md:357-362` widerspricht sich 230 Zeilen später wie in W-4; OpenAPI `:4215-4218` begründet den fehlenden Bewegungswert bei `PATCH` falsch — es ändert auch `todoId` (`routes/time.ts:66`), Begründung auf O-X zeigen lassen. Dazu T-115: `usecases/todos.ts:337` „Die Kanban-Spalte bleibt (E-023)" an der Funktion, die das Gegenteil rechnet; **Sicherheit:** `http/input.ts:111` erfasst `U+200E`, `U+200F`, `U+061C` nicht, obwohl der Regelname seit T-107 als `details[].name` ohne umgebenden Text in einen Löschdialog geht; O-AD (`Todo.title` Hauptabschnitt `:3691` 512 → 500); O-AB aussprechen statt bauen | domain-dev |
| ~~T-118~~ | **Fertig, Auflage 3 erfüllt.** Das Designsystem beschreibt kein Kartenverschieben mehr, sondern die zwei Ziehbewegungen, die es seit E-054 noch gibt, je mit Alternative, plus einen Absatz, warum das Board keine hat; ausgedehnt auf `--shadow-drag`, `--z-drag` und „Statusspalten des Boards". **B-1 an der Ursache behoben:** `setConflict(null)` steht jetzt unmittelbar hinter `await performStop`, und ein danach scheiternder Start meldet „Gebucht, aber der Timer … ließ sich nicht starten" statt den Stopp zu verschlucken. **Zwei Messungen im Browser (E-062), Wegwerfseite danach gelöscht:** B-1 alt = ein Bild mit `scrim:true, toasts:1`, neu = keines; Frist alt = A steht bei t=8,4 s noch, neu = A ist weg. B-2 bei 1280×720: ab fünf Meldungen rollbar, jüngste bei y≈710; Fokus auf dem „Rückgängig" der ältesten von zehn holt `scrollTop` von 897 auf 0, Knopf bei y=130; Geometrie bei drei Meldungen alt wie neu. Mit erledigt: B-4, B-5, B-6, B-7, B-10, beide Kommentarbefunde aus T-115 (`types.ts:581`, `movement.ts:74`/`:32`) und das Fristrisiko `ToastContext.tsx:189`. Neu `apps/web/src/app/undoDone.ts` — B-6/B-7 in **einer** Datei statt drei Abschriften. Nachweis: vier Teilläufe `typecheck` 0, web-Tests 63/0, web build 0, `build:designsystem` 0, `contrast` 0/432. **Nicht gebaut:** B-12 (`inert` nähme auch die `aria-live`-Ansage; T-116 nennt es „kein Befund"), globaler `unhandledrejection`-Auffänger (meldete doppelt oder bräuchte ein Protokollziel, das es nicht gibt). **Neu gefunden:** „Takt beenden" scheitert stumm (`App.tsx:250`, `ServiceStoppedPanel`) — der einzige Ausgang aus der Sperrmeldung; als O-AF. **An e2e-tester (Welle K):** `toast-eviction.spec.ts:84` `toHaveCount(4)` **und** die Schleife „Die drei jüngeren stehen noch" — die Verdrängung bleibt gültig, neu ist die Uhr; Empfehlung `page.clock.install()` vor `gotoBoard` und `pauseAt(...)`. Dazu zwei neue Fälle: A-6.8 (kein Bild mit `.scrim` und `.toast` zugleich) und SC 2.4.7 (siebenmal „Erledigt", Tab bis zur ältesten Meldung, `boundingBox().y >= 0`). Ursprünglich: **Auflage 3 (B-8).** Das Designsystem beschreibt das Verschieben von Karten — es ist die abgenommene Referenz. Dazu B-1 `TimerContext.tsx:550/:556` (Stopp-Meldung beim Timerwechsel entsteht hinter der Abdunklung, einzige Bestätigung der gebuchten Zeit, laufende Frist), B-2 Bildschirmgrenze des Stapels (SC 2.4.7), B-6 `TodoListScreen.tsx:218` (Rückweg ohne `catch`, stumm), T-115: `api/types.ts:581` falscher Rechnungsname, `lib/movement.ts:74`/`:32` zählt zwei Aufrufer statt drei; **Risiko** `ToastContext.tsx:189` Frist an neuer Funktion je Zeichnen — mit e2e-tester absichern (`toast-eviction.spec.ts:84` geht heute nur deshalb auf). Danach B-4, B-7, B-5 | frontend-dev |
| ~~T-119~~ | **Fertig, mit einem Fund, der über den Auftrag hinausgeht.** Fremder Text geht im Aufgabenbereich nicht mehr roh in die Anzeige — neuer Baustein `Foreign` setzt ihn in ein `<bdi>` **und** nimmt ihm die unsichtbaren Zeichen. Beides ist nötig: `unicode-bidi: isolate` allein schützt nur die Umgebung, innerhalb des isolierten Blocks dreht ein `U+202E` weiter um (UBA X2–X5, `bidi-override` hilft auch nicht) — **Berichtigung des eigenen T-114-Vorschlags**. Neun Anzeigestellen behoben statt nur genannt; statische Prüfung findet im Stand `121bf05` **12** rohe Stellen. **Regression gefunden:** T-117 erweiterte die Zeichenklasse an der Tür um `U+061C`, `U+200E`, `U+200F`, `suggestTitle` nicht — die Sackgasse aus T-114 war für drei Zeichen wieder offen, und **Abschnitt 16 bemerkte es nicht, weil er gegen eine abgeschriebene Liste prüft**. Abschnitt 17 fragt jetzt die Tür selbst ab, über die ganze BMP (77 Zeichen). `suggestTitle`, `prepareNote` (4000) und `clip` (40) schneiden an einer Zeichengrenze: Die halbe Ersatzstelle war kein Schönheitsfehler, sondern ein Wert, der den Base64-Weg des Exports nachweislich nicht überstand (`fromBase64(toBase64(t)) !== t`). Entschieden als **E-063**. Nachweis: `typecheck` 0, `test` 787/0, `proof:addin` **164** (vorher 148), `proof:openapi` 105, `proof:all` 0 (863), `boundaries` 0, Add-in-Build 0; drei Gegenmessungen belegen, dass die neuen Prüfungen unterscheiden. **Offen und benannt:** das Vermerkfeld zeigt den übernommenen E-Mail-Text unverändert — ein Eingabefeld, dessen Inhalt zu ändern die Eingabe änderte; derselbe Text wird in `apps/web/**` angezeigt (O-AH). Poolnamen in den Sätzen aus `@takt/domain` sind nicht isoliert (kein Weg aus einer E-Mail dorthin). **An e2e-tester:** `tests/e2e/outlook-addin-build.spec.ts:113` nennt jetzt das falsche Element (`<span class="mono">` → `<bdi class="mono">`); der Locator sucht über die Klasse und trägt weiter. Ursprünglich: Bidi-Isolierung der rohen Betreffanzeige (`TaskPane.tsx:364`, `addin.css`) — ein `U+202E` dreht die Anzeige, ohne durch die Tür zu gehen; `suggestTitle` auf Codepunkte schneiden statt UTF-16-Einheiten | integration-dev |

Danach Welle K: unit-tester und e2e-tester ziehen nach, was Welle J ändert; zuletzt der Dokumentierer (D-1/D-2 Benutzerhandbuch, Glossar, README) — mit der Auflage aus R-2a: beschreiben, nicht zitieren.

### Welle vom 2026-09-04, Welle I (Sicherheitsnacharbeit und Wiedervorlage der Reviewer) — fertig; **beide Reviewer geben Welle E bis H frei**

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-114~~ | **Fertig.** Die Add-in-Tür benutzt jetzt `titleSchema` und `nameSchema` aus `http/input.ts` statt eigener Abschriften — nicht mehr zeichengleich, sondern **dasselbe** Schema; der Kommentar `:74`, der die Gleichheit seit T-101 zu Unrecht zusicherte, ist richtiggestellt. `withoutControlCharacters` ist nicht ausgeführt; statt eine zweite Fassung zu schreiben, hat integration-dev seine beiden ausgeführten Anwendungen genommen — **Folge: Titeldeckel 512 → 500** an der Add-in-Tür. **Orchestrator entscheidet: bleibt bei 500**, kein `export` von `withoutControlCharacters`; 500 ist der Wert von `POST /todos` und `PATCH /todos/{id}`, und ein mit 512 Zeichen angelegtes Todo war in der Hauptanwendung nicht mehr speicherbar — dieselbe Sackgasse, nur über die Länge. `proof-addin.mjs` Abschnitt 16 hält **beide Türen** gegen dieselben 20 Zeichen und neun harmlose, gegen beide Längengrenzen und gegen die laufende Route; Gegenprobe gegen den nachgebauten Stand `71c6695`: 20 von 20 Abweichungen, der Abschnitt wäre vorher rot gewesen. Damit ein Betreff mit unsichtbarem Zeichen nicht in eine Sackgasse führt, bereinigt `suggestTitle` den **Vorschlag** — die Tür weist weiter ab, bereinigt wird nur, was der Benutzer nicht selbst geschrieben hat. Keine weitere Zeichenkette der Add-in-Tür braucht eine zweite Wache; einen Ordnernamen nimmt diese Tür gar nicht entgegen. Nachweis: `typecheck` 0, `test` 787/0, `proof:addin` 148 (vorher 134), `proof:openapi` 105, `proof:all` 0 (13 Pfade, 847 Prüfungen), `boundaries` 0 (321 Dateien), Add-in-Build 0. **Offen in eigener Hoheit (Welle J):** `TaskPane.tsx:364` zeigt `mail.subject` roh — ein U+202E dreht die Anzeige, ohne je durch die Tür zu gehen (`unicode-bidi: isolate`); `suggestTitle` schneidet auf UTF-16-Einheiten und kann ein Emoji halbieren (Vorbestand). **Korrektur am T-112-Befundtext:** `readJson` wird in `routes/addin/index.ts:394` lokal definiert, nicht aus `http/input.ts` geholt; der reale Import über die Modulgrenze ist `statusFor` aus `http/problem.ts:67`. **Altbestand:** vor T-114 angelegte Titel mit solchen Zeichen oder 501–512 Zeichen lassen sich per `PATCH` nicht unverändert zurückschicken (422) — gleicher Nebeneffekt wie T-101 Annahme 6, eine Migration wäre die dort abgelehnte stille Änderung. Ursprünglich: T-112-1: `withoutControlCharacters` (`http/input.ts:121`) auf `title` (`routes/addin/schema.ts:66`) und `tagNames` (`:85`), Kommentar `:74` richtigstellen, der die Gleichheit mit `nameSchema` noch zusichert; Nachweis, der beide Türen gegen dieselbe Zeichenklasse hält | integration-dev |
| ~~T-115~~ | **Freigegeben.** Kein Befund führt zu Datenverlust, doppelter Abrechnung oder einer Regel, die mehr trifft als gesagt. Der Bereich umfasst neun Commits, nicht vier (Welle D, Prüfsummen-Merge und die zwei Typprüf-Vorbereitungen stecken mit darin). Die Zuordnung Vorgang → Rechnung ist an allen acht Stellen **gegen die Schreibpfade** geprüft, nicht gegen die Kommentare: `bookingMovementStates` nur dort, wo „Erledigt" tatsächlich fällt (Timerstart mit verdrängtem eigenem Timer, beide Add-in-Routen mit `clearDone` in derselben Transaktion), `closedEntryMovementStates` bei Stopp, verwaister Buchung und Buchung von Hand, `completionMovementStates` an beiden `/done`-Routen und am Start ohne Buchung. Die T-107-Korrektur an meinem E-061-Nachtrag ist belegt: `repo-time.ts:174-189` schreibt nur in `time_entry`, kein Trigger über alle 24 Migrationen setzt `completed_at`. `null` bedeutet an allen vier Wachen dasselbe. Gemessen: `typecheck` 0, `test` 787/787, `boundaries` 0, `proof:all` 0. **Vier „sollte" (Welle J), alle vier Kommentare, die etwas anderes behaupten als der Code tut:** `apps/web/src/api/types.ts:581` nennt `bookingMovementStates`, benutzt wird `closedEntryMovementStates` (frontend-dev); `apps/local-api/src/usecases/todos.ts:337` „Die Kanban-Spalte bleibt (E-023)" an genau der Funktion, die das Gegenteil rechnet — W-3 einen Stock höher (domain-dev); `apps/web/src/lib/movement.ts:74`/`:32` zählt zwei Aufrufer, es sind drei seit `BookingDialogs.tsx:128` (frontend-dev); OpenAPI `:4195` „sieben" gegen acht Zeilen in der eigenen Tabelle — deckt sich mit B-9 (domain-dev). **Sicherheit:** `http/input.ts:111` erfasst die Bidi-Einbettungen und -Isolate, **nicht** die Marken `U+200E`, `U+200F`, `U+061C`; seit T-107 geht der Regelname als `details[].name` ohne umgebenden Text in einen Löschdialog (domain-dev, security-checker zur Kenntnis). **Risiko:** `ToastContext.tsx:189` hängt die Achtsekundenfrist an eine bei jedem Zeichnen neue Funktion — jede neue Meldung startet die Frist aller stehenden neu, und `tests/e2e/toast-eviction.spec.ts:84` geht heute nur deshalb auf; wer das eine repariert, sichert das andere ab (frontend-dev mit e2e-tester). **Nicht bewertet:** T-114 lag während der Prüfung im Arbeitsbaum. Ursprünglich: Wiedervorlage Code: Welle E bis H (Commits `4d1da1c`, `3282322`, `4dd3171`, `71c6695`) | code-reviewer |
| ~~T-116~~ | **Freigegeben für Welle E bis H, drei Auflagen vor dem Dokumentierer.** Alle fünfzehn R-2a-Hinweise erledigt (dreizehn ganz, zwei mit benanntem Rest); W-11 über drei Pakete ohne Schnitt in fremdem Text, W-13 bis auf zwei Stellen sichtbaren Textes auf der Musterseite, Migrationen richtig als Geschichte belassen. Ebenen-Ordnung aus T-110 in allen Punkten bestätigt: vorher mit der Maus bedienbar und mit der Tastatur unerreichbar (SC 2.1.1), jetzt für beide gleich untätig; eine Meldung mit Rückweg hat keine Frist und läuft hinter dem Dialog nicht ab. SC 1.4.3 greift nicht (Ausnahme „inactive user interface component"). Zwölf Befunde, keiner blockierend. **Auflage 1 erledigt (Orchestrator):** E-060 Punkt 4 nachgezogen — der gebaute Titel „„X“ ist erledigt." schlägt den älteren Wortlaut, weil E-060 für die Form auf E-058 Punkt 6 verweist und W-5 den Namen in den Titel geholt hat. **Auflage 2 (domain-dev, Welle J):** B-9 — `docs/architektur.md:127-128` „drei Vorgänge" und OpenAPI `:4195` „sieben" gegen acht, während die OpenAPI zwei Zeilen tiefer richtig zählt (`:4199-4208`); `architektur.md:357-362` widerspricht sich 230 Zeilen später wie in W-4; dazu OpenAPI `:4215-4218` begründet den fehlenden Bewegungswert bei `PATCH` falsch — es ändert auch `todoId` (`routes/time.ts:66`), und das ist O-X (W-8-Muster: richtige Bauart, falscher Beleg). **Auflage 3 (frontend-dev, Welle J):** B-8 — das Designsystem beschreibt das Verschieben von Karten. Weitere Befunde nach Gewicht: B-1 `TimerContext.tsx:550/:556` (Stopp-Meldung beim Timerwechsel entsteht hinter der Abdunklung, einzige Bestätigung der gebuchten Zeit, mit laufender Frist — einzige Stelle dieser Art), B-2 SC 2.4.7 (ab sieben Meldungen mit Rückweg verlässt die älteste das Fenster, Fokus außerhalb des Sichtbaren), B-6 `TodoListScreen.tsx:218` (Rückweg aus „Erledigt" ohne `catch`, kein globaler Auffänger, Fehlschlag stumm), B-4, B-7, B-5. **O-Q auf vier Befunde geschrumpft** (C-14, C-16, C-21, A-4.4 halb); C-12 und C-13 führte R-2a zu Unrecht als offen. Ursprünglich: Wiedervorlage Spezifikation und Bedienkonzept: Welle E bis H, dazu Toast-Titel „„X“ ist erledigt." gegen E-060 Punkt 4, Wortlaut „Gebucht: <Dauer>." und „Betroffen sind die Regeln …", Ebenen-Ordnung nach T-110 | spec-ux-reviewer |

Danach Welle J: documenter (D-1/D-2 Benutzerhandbuch, Glossar, README) als Letzter.

### Welle vom 2026-09-04, Welle H (Nachzügler, Tests, Sicherheit) — fertig; `test:e2e` 56/56, `test` 787/787

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-110~~ | **Fertig.** W-11: Der Satz entsteht aus `details[].name` („Betroffen sind die Regeln „Ost“, „Nord“ und „Abrechnung“."), Gattungswort entscheidet der Satz, nicht der Eintrag; ohne `name` Zeichen für Zeichen der T-097-Wortlaut, im Dateikopf als Vertragsfall begründet. Nebenbefund: `TaktApiError.details` war strukturell zuweisbar und hätte das vierte Feld verschluckt — `client.ts` nachgezogen. **TP-KANBAN-02 ist kein Zufall:** bei 1280×720 verdeckt schon eine Meldung den Mittelpunkt des Knopfes „Anlegen" (`document.elementFromPoint` liefert `li.toast`), bei 1024×768 die zweite; der Stapel tritt jetzt hinter die Abdunklung, solange ein Dialog offen ist (`body:has(.scrim) .toast-layer`), danach 42/42 Prüfpunkte auf dem Knopf. Designsystem Abschnitt 5.1 „Ebenen" neu. Bildschirmgrenze und T-112-H3 bewertet und begründet **nicht** gebaut. Nachweis: `typecheck` 0, web-Tests 63/0, web build 0; kein `dangerouslySetInnerHTML`/`innerHTML`/`eval` in `apps/web`, `apps/desktop` (0 Treffer). **Risiken:** ab sieben Meldungen mit Rückweg fällt die älteste aus dem Fenster (807 px Stapel bei 704 px Platz); `:has()` ist tragend. **Orchestrator erledigt:** Kommentar an `--z-toast` in `packages/ui-tokens/tokens.css` nachgezogen und die Hoheitslücke geschlossen — `packages/ui-tokens/**` gehört ab jetzt frontend-dev (`CLAUDE.md`). Ursprünglich: W-11-Verbrauch: `errorText.ts:98-104` liest `details[].name` („Betroffen sind die Regeln „Ost“, „Nord“ und „Abrechnung“."), Rückfall auf `message` ohne `name`; T-109-Beobachtung TP-KANBAN-02 (Toast verdeckt Dialogknopf „Anlegen") prüfen; T-108 Frage 2 (Bildschirmgrenze Stapel) beantworten | frontend-dev |
| ~~T-111~~ | **Fertig.** 29 neue Fälle, 758 → 787, Abdeckung 84,51 % Zweige, alle drei Schwellen gehalten, `typecheck:test` 0, `test` 0. Neu: `apps/local-api/test/usecases/time-entry-movement.test.ts` (4), `apps/web/test/lib/labels.test.ts` (13), `apps/local-api/test/routes/addin/service.test.ts` (6); erweitert `mappers.test.ts` (Vertrag `TaktFieldError` vollständig), `errorText.test.ts` (5 Fälle gegen `details[].name`), W-13 in `board.test.ts`. **T-112-H2 erledigt:** 14 rohe Steuer-/Bidi-Zeichen in `test/http/input.test.ts` als `\uXXXX`-Escapes, Datei wieder Text (`file` belegt UTF-8), Codepunkt-Scan 0 Treffer, gleiches Verhalten. **Blockade:** `ToastContext.evict()` nicht als Einheit prüfbar — kein React-Testrahmen, `evict` nicht ausgeführt; entschieden als **E-062** (Bausteine im Browser prüfen, kein jsdom; T-108-Messung und T-113-Fall decken es). **Klarstellung:** Die im Bericht erwähnte „parallele Commit von frontend-dev" gab es nicht — es war die unkommittierte T-110-Arbeit im Baum; letzter Commit ist unverändert `4dd3171` des Orchestrators. Ursprünglich: `mappers.test.ts:362` auf `name`; `poolReference` mit und ohne `name`; `createTimeEntry` mit `poolMovement` (Bewegung und `null`); `poolPlacementMessage`/`reactivationTitle`; `evict()` W-10; Attrappen-Test Add-in-Routen `poolMovement` (T-104); W-13 `packages/domain/test/board.test.ts:14`; `errorMessageWithRules` mit `name` (nach T-110-Vertrag) | unit-tester |
| ~~T-112~~ | **Fertig, freigegeben bis auf einen Punkt.** Bedrohungsmodell: 14.2 und 15.3 als Protokoll belassen und nachgetragen, heutiger Stand in neuem Abschnitt 16 (16.1–16.9). VG-2 unverändert bewertet: ein Feld statt dreier, dieselbe Datenklasse. `POST /time-entries` rechnet in derselben Transaktion, nur eigene Regelnamen, `closedEntryMovementStates` ist die vorsichtigere Rechnung. Sechs von sieben R-3a-Hinweisen erledigt, S-2 und S-3 auch; S-1 offen. Semgrep CLI 1.166.0 über 97 Dateien: 156 Regeln, 0 Befunde. Semgrep Guardian zum sechsten Mal nicht erreichbar, 42Crunch weiter nicht betriebsbereit — kein Ersatz vorgetäuscht. **Befund T-112-1 (sollte):** `routes/addin/schema.ts:66` (`title`) und `:85` (`tagNames`) prüfen nicht auf C0/C1 und Bidi; `checkName` deckt sie nicht ab (`tag-name.ts:147`), der Titel ist mit `mail.subject` vorbelegt (`TaskPane.tsx:120`), `todo.title`/`todo.tags` sind Feldquellen einer Exportvorlage. Kein XSS-Weg, aber Anzeige mit entferntem Urheber → `withoutControlCharacters` (`http/input.ts:121`) auf beide Felder, Kommentar `:74` richtigstellen (integration-dev, Welle I). Hinweise: T-112-H1 acht Aufrufstellen, keine Obergrenze für die Zahl der Regeln (Auftraggeber, jetzt O-Z); T-112-H2 rohe Steuerzeichen in `test/http/input.test.ts` machen die Datei für Git binär (unit-tester, an T-111 nachgereicht); T-112-H3 Namen als eigene Knoten statt zusammengefügtem Satz (frontend-dev, an T-110 nachgereicht). Ursprünglich: Bedrohungsmodell `:2403-2404`, `:2638-2639` auf `poolMovement`; 15.x für die eine Form; `details[].name` als neue Ausgabestelle des Regelnamens (T-107 Risiko 3, Wache `http/input.ts` H-2); `POST /time-entries` mit `poolMovement`; Freigabe Welle E–G | security-checker |
| ~~T-113~~ | **Fertig.** Neu `manual-booking-movement.spec.ts` (erste Buchung mit Bewegung, Gegenprobe ohne, erledigtes Todo ohne Verlassen der Erledigt-Spalte — A-2.5) und `toast-eviction.spec.ts` (Meldung mit Rückweg bleibt, älteste ohne Aktion weicht, Schließen wirkt weiter); `tag-folder-rule-lock.spec.ts` auf den T-110-Wortlaut, für alle drei Löschflächen (Ordner, Tag, Status — dieselbe Funktion). Testplan Abschnitt 21: TP-EXPST-14, TP-TOAST-01, TP-TAG-15. **Voller `test:e2e`: 56/56** (Marke 51/51 nach `4dd3171`, plus fünf neue Fälle), gemessen mit T-107/T-108/T-110 im Baum — damit ist der zusammengesetzte Wellen-H-Lauf erledigt. Kein roter Lauf gemeldet, weil keiner stattfand; das ist so richtig. Rückfallmarke: `tag-folder-rule-lock.spec.ts:174, :238, :265-303, :346` würde bei Rückkehr zum alten Wortlaut zu Recht rot. Ursprünglich: *nach dem Gesamtlauf:* Buchung von Hand mit Bewegung und Gegenprobe `null` (Toastzeile), W-10 (eine Meldung mit „Rückgängig", vier ohne), Regelnamen-Satz nach T-110, Befunde des Gesamtlaufs | e2e-tester |

Danach Welle I: code-reviewer und spec-ux-reviewer über Welle E–H (Toast-Titel „„X“ ist erledigt." gegen E-060 Punkt 4, Wortlaut „Gebucht: …"); zuletzt documenter.

### Welle vom 2026-09-04, Welle G (Buchung von Hand, Reviewer-Hinweise, End-to-End nachziehen) — fertig; Gesamtlauf `test:e2e` nach Commit `4dd3171`: 51/51 grün (1,7 min), Review in Welle I

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-107~~ | **Braucht Review.** `POST /time-entries` liefert `poolMovement` flach neben der Buchung (`TimeEntryAfterBooking`), in derselben Transaktion, Anlaß `'booking'`, Rechnung `closedEntryMovementStates` über `movementOfBooking` — **nicht** `bookingMovementStates`, weil die Buchung von Hand „Erledigt" nicht aufhebt (A-2.5; `repo-time.ts:174`); Nachtrag zu E-061 vom Orchestrator richtiggestellt (Frage 1 = A). Bauteil `PoolMovement` nennt alle acht Vorgänge mit Anlaß (F2). W-8 an drei Stellen (`ux_pool_name` UNIQUE NOCASE seit 0001). W-11: `TaktFieldError.name`, einziger Bildungsort `poolReference`, alle drei Sperren erben; `tag_name_ambiguous` ohne `name` (Frage 3: bleibt so — dort ist es ein Tagname, keine Regel). W-13 zwölf Stellen. `proof:openapi` 100 → 105. Nachweis: `typecheck` bis auf `apps/web/src/showcase/BoardSection.tsx` (T-108 in Arbeit) 0, `proof:all` 0, `boundaries` 0, `test` 757/758 — rot nur `packages/storage/test/mappers.test.ts:362` (fehlt `name: 'Abrechnung'`, unit-tester Welle H). **Sicherheit:** `details[].name` neue Ausgabestelle des Regelnamens, Wache bleibt `http/input.ts` (H-2) — an security-checker. Frage 2 als O-X. Ursprünglich: O-V: `POST /time-entries` liefert `poolMovement` (E-061 Nachtrag) samt OpenAPI und Anwendungsfall; W-8 Kommentare in `packages/domain/src/pool-movement.ts:76-80` und `usecases/pool-movement.ts:88` richtigstellen; W-11 `details[].name` als eigenes Feld; W-13 in OpenAPI `:949, :1122, :1308, :3030`, `docs/datenmodell.md:84, :755, :1807`, `docs/architektur.md:335`, `board.ts:12`, `todo.ts:5`, `tag.ts:197`, `usecases/board.ts:11`; T-104-F2 OpenAPI `:4123` „drei Vorgänge"; `service-scenario.mjs:757` Prosa | domain-dev |
| ~~T-108~~ | **Fertig.** `CreateTimeEntryResult` flach mit Pflichtfeld `poolMovement: PoolMovement \| null` (kein `?? null`), Toast „Zeit gebucht auf „X“." mit „Gebucht: <Dauer>." plus Bewegungssatz wie der Stopp; W-9 `ReactivationNotice` samt CSS und Musterstellen weg, Musterseite mit echtem `ToastProvider` zeigt Toast und Etikett; W-10 `MAX_TOASTS = 4` und `evict()` überspringt Meldungen mit Rückweg (Stapel darf darüber wachsen, wenn nur solche darinstehen); W-13 vier Stellen; W-14 `poolPlacementMessage` und `reactivationTitle` in `labels.ts`, Board und Regelliste eine Fassung. Nachweis: `typecheck` 0, web build 0, contrast 0/432, `boundaries` 0, `verify:bundle` 0, web-Tests 0. **Fragen an Welle H:** Wortlaut „Gebucht: …" an spec-ux-reviewer; Tagesgruppe/Exportwert auch im Handbuchungs-Toast? (Auftraggeber, O-Y); Bildschirmgrenze für viele Rückweg-Meldungen? unit-tester: `poolPlacementMessage`/`reactivationTitle`. e2e-tester: Buchung von Hand mit Bewegung, W-10 (eine mit „Rückgängig", vier ohne). Ursprünglich: O-V-Verbrauch: Toast „Zeit gebucht." mit Bewegungssatz (`BookingDialogs.tsx:107`); W-9 `ReactivationNotice` aus der Musterseite; W-10 Verdrängung schont Meldungen mit Aktion (`ToastContext.tsx:87`); W-13 `Kanban.tsx:144`, `DESIGNSYSTEM.md:623`, `scripts/contrast-check.mjs:245`; W-14 Toastpaar Board/Regelliste | frontend-dev |
| ~~T-109~~ | **Fertig.** `support/api.ts` mit `poolMovement: PoolMovementNames \| null` (lokaler Typ), `pool-movement-sentence.spec.ts` liest die Bewegung direkt; „Kein Treffer"-Fall erwartet drei leere Listen, weil das Todo dort erledigt ist (an `routes/addin/service.ts:232` geprüft); neuer Fall TP-EXPST-12a (offenes Todo mit offener Buchung → `null`, nur Dienst, O-P-Hinweis); W-13 in `kanban.spec.ts`, `todo-revival.spec.ts`, Testplan Abschnitt 20. **Beobachtung:** TP-KANBAN-02 einmal 60 s Zeitüberschreitung — Toast verdeckte Dialogknopf, danach zweimal grün; an frontend-dev (W-10-Nachbarschaft), nicht gedeutet. Ursprünglich: Add-in-Helfer und `pool-movement-sentence.spec.ts` auf `poolMovement`, neuer Fall Vorschau auf offenem Todo mit offener Buchung → `null`, `docs/testplan.md:21, :838, :2313-2314`, W-13 in `kanban.spec.ts:16`, `todo-revival.spec.ts:16, :124`; voller `test:e2e`-Lauf | e2e-tester |

### Welle vom 2026-09-04, Welle F (Add-in auf eine Form, Tests nachziehen) — fertig

Stand `4d1da1c`: `typecheck` 0, `test` 0 (684), `proof:all` 0, `test:e2e` 40/45 (fünf Wortlaute).
Drei Agenten parallel, disjunkte Pfade. integration-dev ändert den Vertrag der Add-in-Routen —
e2e-tester läuft deshalb nur gezielt (`--grep`), nicht die ganze Reihe; die Add-in-Helfer in
`support/api.ts` zieht er erst in Welle G nach.

| Nr | Aufgabe | Wer |
|---|---|---|
| ~~T-104~~ | **Fertig.** Beide Add-in-Routen liefern `poolMovement: PoolMovement \| null` (`AddinTodoMatch`, `AddinBookResult.booked`), gerechnet über `bookingMovementStates` + `poolMovementNamer` in neuer Hilfsfunktion `bookingMovement`; lokales `BOOKING_EFFECT` und Handpaare weg; `null`-Regel wie `movementOfStart` (offenes Todo mit bestehender offener Buchung), verzögerter Namensgeber in `findMatches`. Add-in: `offerMovement` ersatzlos weg, `NOTHING_MOVED` beim Wiederöffnen, `PoolMovement` als `import type` aus `@takt/domain`. OpenAPI Add-in-Abschnitt: Form, W-1, W-15. `proof-addin.mjs` mit zwei Schlüsselvergleichen „eine Form, keine Reste". **Abweichung gemeldet:** `apps/local-api/scripts/proof-openapi.mjs:452-466` und `proof-addin-wiring.mjs:319-333` (domain-dev-Hoheit) als Vertragsfolge angepasst — angenommen. Nachweis: `typecheck` 0, `test` 758/0, `proof:addin` 134/0, `proof:openapi` 100/0, `proof:addin-wiring` 32/0, `proof:all` 0, `boundaries` 0, Add-in-Build 0. Rot durch Vertragswechsel (Welle G): `tests/e2e/support/api.ts:570-572, :595-597`, `pool-movement-sentence.spec.ts:120-124, :136-138, :148-150, :232-234, :243-245`, `docs/testplan.md:2313-2314` (e2e-tester); `docs/bedrohungsmodell.md:2403-2404, :2638-2639` (security-checker, Welle H); `service-scenario.mjs:757` Prosa und OpenAPI `:4123` Bauteil `PoolMovement` „drei Vorgänge" → sieben (domain-dev). Lokales Taskpane-Bündel unter `src-tauri/taskpane/` ist ignoriert, nicht eingecheckt — kein Befund. Ursprünglich: E-061 Punkt 3: Add-in-Routen liefern `poolMovement: { appears, enters, leaves } \| null` statt `poolNames`/`enteringPoolNames`/`leavingPoolNames`; Rechnung über `bookingMovementStates(todo, { hasOpen, hasExported })` aus `usecases/pool-movement.ts`, lokales `BOOKING_EFFECT` in `routes/addin/service.ts:246` und beide Handpaare entfallen; Add-in (`apps/outlook-addin/**`) liest `poolMovement` und ruft `poolMovementSentence` wie bisher; OpenAPI Add-in-Abschnitt: Form, W-1 (`:2961-2963` gestrichener Satz), W-15 („die Pools" → „Regeln — Pools wie Board-Spalten"); `proof:addin` nachziehen | integration-dev |
| ~~T-105~~ | **Fertig.** 74 neue Prüffälle (684 → 758): `movement.test.ts`, `pool-movement-states.test.ts`, `todo-done-movement.test.ts`, `timer-orphan-resolution.test.ts`, `http/input.test.ts` (neu), `timer.test.ts`, `mappers.test.ts`, `repo-tags.test.ts`, `migration-runner.test.ts` (erweitert). `AppContext`/`UnitOfWork`-Attrappe zweimal bewusst dupliziert. Rotnachweis über falsche Erwartungen, nicht über `src`. Abdeckung 84,25 % → 84,51 %. `typecheck` 0, `test` 0, `test:coverage` 0. **Vorher/Nachher-Messung per `git stash`/`stash pop`** — das nimmt den ganzen Arbeitsbaum, auch die Dateien von integration-dev und e2e-tester, die parallel liefen: **nicht wiederholen** (Vorher-Wert aus dem Board oder aus dem letzten Commit nehmen). Ursprünglich: Einheitentests: `apps/web/src/lib/movement.ts`; `completionMovementStates`/`switchTodoDone` (E-060, `null` bei unverändertem Kennzeichen); `closedEntryMovementStates` gegen `bookingMovementStates` (Stopp ohne `completed_at`, T-101 Annahme 1); `orphan_discarded` durchgereicht; `input.ts` H-2 (C0/C1, U+202A–U+202E, U+2066–U+2069 abgewiesen, gültige Umlaute/Emoji nicht); `details`-Obergrenze 21/20; `TagPort.remove` mit `details`; `legacy_alter_table` im `finally` (Fehlerpfad). Kein Rotnachweis über Mutation von `src` | unit-tester |
| ~~T-106~~ | **Fertig.** Fünf Erwartungen auf die gemessenen Wortlaute, Tag-Fall auf „Regelname vorhanden" gedreht; `TodoDoneResult extends Todo` in `support/api.ts`; neu `tests/e2e/done-movement-announcement.spec.ts` (TP-EXPST-13, nur Detailansicht) und TP-TIMER-11 (`orphan_discarded`); `docs/testplan.md` Abschnitt 19, W-13-Stellen umformuliert. Gezielt 12/12, `typecheck:e2e` 0; kein voller Lauf (Add-in-Umbau läuft). Frage: TP-EXPST-13 um Board/Liste erweitern? Ursprünglich: End-to-End: fünf rote Erwartungen auf die gemessenen Wortlaute aus `reports/T-102-frontend-dev.md` Abschnitt 2 (`timer-stop-announcement.spec.ts:103-104, :173-176, :256`, `tag-folder-rule-lock.spec.ts:152, :213`), Helfer `setTodoDoneWithMovement`/`reopenTodoWithMovement` auf die gemessene Hülle (`TodoDoneResult extends Todo`, `poolMovement` daneben), die Entwürfe aus `reports/T-103-e2e-tester.md` für E-060 und O-R als Spezifikationen, `docs/testplan.md` Abschnitt 19 und W-13-Stellen `:21, :838`. Nur gezielte Läufe; Add-in-Spezifikationen nicht anfassen | e2e-tester |

Danach Welle G: domain-dev (O-V, W-8, W-11, W-13-Stellen), frontend-dev (O-V-Verbrauch, W-9, W-10,
W-13, W-14), e2e-tester (Add-in-Helfer auf `poolMovement`, voller Lauf); Welle H Reviewer; zuletzt
documenter.



**Beim Auftraggeber:** `docs/spec.md` — Drag & Drop in Zeilen 84, 257, 306 seit E-054 aufgehoben;
A-3.5, A-3.6, A-5.7 nachtragen (Vorschlag in R-2); A-13.6 klären.

## Offen — Restpunkte, keine Blocker

| Nr | Punkt | Wer |
|---|---|---|
| O-A | Kanban-Spalten lassen sich nicht umbenennen — es gibt keine Bedienmöglichkeit dafür (T-052) | frontend-dev |
| O-B | `PoolWrite` kennt kein `position`; jeder neue Pool entsteht auf 0. Fachliche Frage: sollen Pools sortierbar sein? | Auftraggeber |
| O-C | `GET /settings` belegt keine Merkmale zum Datenbankpfad, anders als beim Exportordner | domain-dev |
| O-D | `Pool.rule` heißt weiter `rule`, enthält aber nur noch die erforderlichen Tags. Umbenennen berührt drei Hoheiten — eigene Aufgabe | Orchestrator |
| ~~O-F~~ | *erledigt.* Alle 13 Nachweispfade laufen seit T-08x in `pnpm check` (`proof:all`), zuletzt gemessen nach T-088: 818 Prüfungen, 0 rot |
| ~~O-G~~ | *erledigt in T-084.* Der Poolsatz erscheint nur im Wiederöffnen-Fall. Für ein **offenes** Todo liefert der Dienst `poolNames`, die niemand liest. Der Agent hielt das für vollständig gedeckt, weil nur dort etwas *verschwinden* kann — aber **erscheinen** kann auch ohne Wiederöffnen: Die erste Buchung auf einem Todo ohne Buchung setzt `hasOpenEntries` von falsch auf wahr, und eine Spalte `exportState: 'open'` nimmt es damit auf. `bookingStates` rechnet das bereits richtig; nur die Anzeige fehlt | frontend-dev |
| O-E | Soll das **Ziehen für reine Status-Spalten** zurückkommen? Der Status ist eine Eigenschaft, kein Tag; das wäre umkehrbar, ohne E-054 zu verletzen | Auftraggeber |
| O-M | Die Aufruferseite des Add-ins ist von `proof:callers` nicht erfasst | domain-dev |
| O-N | Die Quellkarten des Add-ins gehen in die Auslieferung mit (1,1 MiB, über HTTPS abrufbar). Nichts wurde stillschweigend gefiltert — die Frage gehört entschieden. | Auftraggeber |
| O-O | `pnpm desktop` braucht jetzt beide Ports frei (17843 und 17844). Verhaltensänderung durch den mitlaufenden Nachweis; die Kette bleibt sauber stehen und nennt den Grund. | — |
| O-P | Neun End-to-End-Fälle nicht gelaufen: Add-in (kein Office.js-Wirt), drei Hüllenzustände (kein echter Tauri-Prozess), Stichprobe über die 19 Orte mit Exportstatus, Standard-Tags über die Oberfläche | e2e-tester |
| O-Q | 14 Befunde aus T-025 unverändert offen, geordnet nach Gewicht ab C-12 | spec-ux-reviewer |

| ~~O-I~~ | erledigt mit T-089/T-093. Restfrage: `resolveRule`/`resolveExcluded` zugunsten von `resolveAxes` streichen? Kommentar an `packages/storage/src/ports.ts` begründete `resolveRule`/`resolveExcluded` mit einem Aufrufer in `routes/addin`, den es seit T-086 nicht mehr gibt; einziger Nutzer in `src` ist der Adapter, dazu `repo-tags.test.ts`. Entweder Kommentar richtigstellen oder beide zugunsten von `resolveAxes` streichen | domain-dev |
| O-J | `resolved` trägt drei Wahrheitswerte (`isEmpty`, `unresolvedRequired`, `matchesNothing`); frontend-dev schlägt einen benannten Grund `matchesNothingReason: 'none' \| 'empty' \| 'unresolved-required'` vor, damit ein dritter Grund die Oberfläche rot statt still macht | domain-dev |
| O-K | Das Add-in kann nicht sagen, *warum* ein Pool im Aufgabenbereich fehlt (bekommt nur Namen). Produktfrage: reicht das? | Auftraggeber |
| ~~O-S~~ | *erledigt (Welle E/F).* **E-061, T-101.** Das Zustandspaar „Wirkung einer Buchung" (`completedAt: null`, `hasOpenEntries: true`) wird an vier Stellen gebildet (Add-in zweimal, `timer/start`, `timer/stop`+`orphaned/resolve`). Vorschlag T-092: `bookingMovementStates(todo, entries)` in `usecases/pool-movement.ts` | domain-dev |
| ~~O-T~~ | *erledigt (Welle E/F).* **E-061 Punkt 3, Welle F (integration-dev).** Add-in-Routen liefern `poolNames`/`enteringPoolNames`/`leavingPoolNames`, Timer-Routen `poolMovement: {appears, enters, leaves}`. Eine Form, zwei Hoheiten plus OpenAPI | Orchestrator |
| ~~O-U~~ | *erledigt (Welle E/F).* **E-060, T-101/T-102.** `PUT`/`DELETE /todos/{todoId}/done` liefern kein `poolMovement`; der Board-Toast nach „Erledigt" schweigt deshalb über Spalten. Begründung aus E-058 Punkt 6 gilt wörtlich — dritter Anlass oder Wiederverwendung von `'booking'` (Bewegung ohne „wieder")? | domain-dev |
| ~~O-R~~ | *erledigt (Welle E/F).* **T-101 (unterscheiden).** `POST /timer/orphaned/resolve` verspricht `reason: [timer_too_short, orphan_discarded]`, der Dienst liefert ausnahmslos `timer_too_short` (T-093). Entweder unterscheiden oder Aufzählung kürzen | domain-dev |
| O-L | `scripts/**/*.mjs` (Nachweispfade, Attrappen) sieht kein Übersetzer; ein `matchesPool`-Aufruf ohne Pflichtfeld bleibt dort still. Option: `checkJs` mit JSDoc-Typen für die Skripte | Orchestrator |
| O-V | `POST /time-entries` (Buchung von Hand) liefert kein `poolMovement`, obwohl es die erste Buchung sein kann; „Zeit gebucht." in `BookingDialogs.tsx:107` nennt das Todo anders als der Stopp (T-102 Frage 1). E-058 Punkt 6 gilt wörtlich | domain-dev, dann frontend-dev |
| O-W | R-2a-Hinweise ohne Zuweisung im Board bisher: W-8 Kommentare `packages/domain/src/pool-movement.ts:76-80`, `usecases/pool-movement.ts:54-60` begründen mit „zwei Pools dürfen denselben Namen tragen" — `ux_pool_name` UNIQUE NOCASE sagt nein (domain-dev); W-9 `Timer.tsx:196` `ReactivationNotice` nur noch auf der Musterseite (frontend-dev); W-10 `ToastContext.tsx:87` `slice(-3)` verdrängt auch Meldungen mit „Rückgängig" (frontend-dev); W-11 `details[].name` als eigenes Feld, damit die Oberfläche „die Regeln „Ost“, „Nord“" sagen kann (domain-dev, dann frontend-dev); W-13 Restvorkommen „Regel über Tags" (`Kanban.tsx:144`, `DESIGNSYSTEM.md:623` frontend-dev; OpenAPI `:912, :1085, :1271`, `docs/datenmodell.md:84, :747, :1790`, `board.ts:12`, `todo.ts:5`, `tag.ts:197`, `usecases/board.ts:11` domain-dev; `docs/testplan.md:21, :838` e2e-tester); W-14 Toastpaar `BoardScreen.tsx:218` gegen `TagsScreen.tsx:492-493` — Board-Fassung auf beiden Flächen (frontend-dev); W-15 OpenAPI `:3023-3060` „die Pools" → „Regeln — Pools wie Board-Spalten" (integration-dev, mit E-061 Punkt 3) | siehe Punkt |
| O-X | **T-117 stellt die Frage vollständig:** Was meldet `PATCH /time-entries/{id}` bei einem `todoId`-Wechsel — nichts, zwei Bewegungen mit Todo-Kennung, oder nur die des aufnehmenden Todos? Eine zweite Bewegung verlangte eine Todo-Kennung in den Namenslisten und berührte damit E-058 Punkt 4. Ursprünglich: `PATCH /time-entries/{id}` erlaubt `todoId`; das Umhängen legt die Achse „hat offene Buchungen" an **zwei** Todos in entgegengesetzte Richtungen um. Bewußt ohne `poolMovement` (E-061 Nachtrag); soll die Oberfläche dort etwas sagen, braucht es eine Form für zwei Bewegungen | Auftraggeber |
| O-Y | Soll der Toast nach der Buchung von Hand wie der nach dem Stopp auch Tagesgruppe und gerundeten Exportwert nennen (T-108 Frage 1)? **T-116 teilt den Punkt:** der gerundete Exportwert ist Geschmack und bleibt hier; der Hinweis auf eine Tagesgruppe ohne Leistung folgt aus E-034 und ist als B-4 ein Befund, keine Frage | Auftraggeber |
| O-Z | Bleibt es bei 200 Ordnertermen je Liste und **keiner** Obergrenze für die Zahl der Regeln? Die Bewegung wird inzwischen an acht Stellen gerechnet, bei `POST /time-entries` innerhalb einer schreibenden Transaktion (T-112-H1) | Auftraggeber |
| O-AA | **Produktfrage (T-116 B-3).** Zeit auf ein erledigtes Todo zu buchen hat je nach Weg zwei Ergebnisse: aus dem Add-in hebt es „Erledigt" auf, in der Hauptanwendung nicht. Beides ist so gebaut, wie es entschieden wurde, aber die Frage lag nie vor, und die Aufhebung durch die Add-in-Buchung hat **keine** Anforderungs-ID. Soll das Nachtragen einer Zeit ein erledigtes Todo wieder öffnen? Ergebnis als A-2.7 bzw. A-10.11 in die Spezifikation | Auftraggeber |
| O-AB | **T-115 Frage.** Soll `POST /timer/start` mit `stopRunning: true` die Bewegung des **verdrängten** Todos melden, oder wird das ausdrücklich ausgeschlossen? Die Hauptanwendung ist nicht betroffen (sie stoppt und startet getrennt), die OpenAPI schweigt. Vorschlag Orchestrator: nicht bauen, sondern aussprechen — eine Antwort trägt eine Bewegung, die des gestarteten Todos | domain-dev |
| O-AC | **T-115 Frage.** `TimerContext.undoReactivation` sind drei Aufrufe ohne gemeinsame Klammer: Meldung präzisieren oder eigene Route? | frontend-dev, dann domain-dev |
| O-AD | `Todo.title` im **Haupt**abschnitt der OpenAPI (`takt-local-api.yaml:3691`) trägt weiter `maxLength: 512`, während `TodoCreate`/`TodoUpdate` 500 tragen. Die 512 gab es nur über die Add-in-Tür; seit T-114 an keinem Eingang mehr (T-114 Frage 2) | domain-dev |
| O-AE | **Sicherheit (T-117 R2).** `apps/local-api/src/access/session-secret.ts:85` prüft den Windows-Benutzernamen nur gegen C0 und DEL — kein C1, keine Richtungszeichen. Der Name geht als `WindowsUser` **unverändert in die Exportdatei**. domain-dev hat ihn nicht geändert, weil es das Verhalten am stdin-Handschlag ändert und `proof:access` daran hängt; der irreführende Verweis in `input.ts` ist richtiggestellt. Entscheidung Orchestrator: **ja, Klasse übernehmen** — eigene kleine Aufgabe mit Prüffall in `proof:access`, Welle K | domain-dev |
| O-AF | „Takt beenden" scheitert stumm (`apps/web/src/app/App.tsx:250`, `ServiceStoppedPanel`) — es ist der **einzige** Ausgang aus der Sperrmeldung, und die Auskunft müsste ins Feld selbst (T-118 Frage 2). Dieselbe Klasse wie B-6 | frontend-dev |
| O-AG | B-11: `listPools` wartet auf den Export aus `@takt/domain`, damit die Aufzählungsform einen Ort hat (T-110 Frage 1, T-118 Frage 3) | domain-dev, dann frontend-dev |
| O-AH | Die Anzeige der Todo-Notiz in `apps/web/**`: Der aus dem Add-in übernommene E-Mail-Text wird dort angezeigt und ist nicht isoliert — dieselbe Frage wie E-063, eine Fläche weiter (T-119 Frage 2) | frontend-dev |
| O-AI | Soll die Zeichenklasse **einmal** im Baum liegen (`packages/domain`; das Add-in führt es bereits als Abhängigkeit)? integration-dev hält die gemessene Doppelung für tragbar, die geteilte Fassung für besser — nach der T-117-Regression spricht mehr für geteilt (T-119 Frage 1) | domain-dev |
| O-AJ | Die Hülle (`apps/desktop`) sagt dem Benutzer nicht, **warum** der Start scheitert, wenn der Windows-Benutzername die Zeichenprüfung nicht besteht (`user_invalid`, seit T-122). Abweisen ist entschieden — aber ein Benutzer kann seinen Windows-Namen nicht ändern und steht sonst vor einer stummen Tür | frontend-dev |
| O-AK | Nachziehen auf die geteilte Fassung aus T-122, in **einer** Welle: integration-dev `apps/outlook-addin/src/text/hidden.ts` samt zwei Nachweisen, frontend-dev die drei Aufzählungsfassungen in `apps/web` auf `enumerateNames`; unit-tester ersetzt danach die doppelte Randmessung durch eine echte Gleichheitsprüfung (T-121 Frage 3, E-063 Punkt 4) | Welle L |
| O-AL | `MAX_TITLE_CHARACTERS = 500` steht zweimal im Baum (Add-in-Bündel und `titleSchema`) — gemessen, aber doppelt. Eine Zahl in `packages/domain` wäre die saubere Antwort; dieselbe Klasse wie die Zeichenklasse vor T-122 (T-123 Frage 1) | domain-dev |
| O-AM | **Entschieden nach T-124 R3 und Frage 3:** `apps/web` bekommt einen statischen Nachweis „kein roher fremder Wert im JSX" nach dem Vorbild von `proof-addin.mjs` Abschnitt 17. Eine Behandlung am Anzeigebaustein ist an der Aufrufstelle unsichtbar — genau die Bauart, die E-063 Punkt 5 messen will. Skript unter `apps/web/scripts` (frontend-dev), Eintrag in `proof:*` durch den Orchestrator | frontend-dev |
| O-AN | `poolMovementSentence` trägt Poolnamen ungeschützt; die saubere Stelle wäre `packages/domain`, ändert aber vierzehn zeichengenau geprüfte Sätze (T-124 Frage 2). Kein Weg aus einer E-Mail dorthin — `nameSchema` an ihrer eigenen Tür | domain-dev |
| O-AO | Gehört die Frage nach dem Windows-Namen in `apps/desktop/src/shell.ts` statt in `apps/web`? Dann bräuchte `@takt/desktop` eine Abhängigkeit auf `@takt/domain` (`package.json` = Orchestrator). Heute lebt der Name in `apps/web` nur für die Dauer eines Funktionsaufrufs (T-124 Frage 1) | Orchestrator |
| O-AP | Soll ein Wächter rohe Steuerzeichen **dauerhaft** aus dem Produktivcode halten? Drei Agenten sind in drei Wellen über dasselbe Zeichen gestolpert, zuletzt der, der es beheben sollte, dreimal in einer Aufgabe. domain-dev hat ihn nicht gebaut, weil die Wahl des Nachweispfads Hoheitsfrage ist (T-126 Frage 1). Verwandt mit O-AM | domain-dev |
| O-AQ | `headersTimeout` im Betrieb heruntersetzen oder hinnehmen? Ein halber Anfragekopf bleibt im laufenden Betrieb 60 s möglich — Betriebsmittel, nicht Lebensdauer (T-126 Frage 2) | domain-dev |
| O-AR | Reste aus T-128: die drei `4000`-Zahlen (zwei bei integration-dev, eine davon eine Doppelung **innerhalb** von `routes/addin/schema.ts`), fehlendes `.gitattributes`, gleiche Fristen für den Aufgabenbereich auf 17844, `tagIds: max(200)` an zwei Türen, Homoglyphen vom Wächter nicht erfasst | domain-dev, integration-dev |
| O-AS | `MAX_TITLE_CHARACTERS` im Add-in nachziehen (`export { MAX_TITLE_CHARACTERS } from '@takt/domain'`) und `proof:addin` Abschnitt 16 vom **Zahlenvergleich** auf die **Herkunftsfrage** umstellen (T-128 Nächster Schritt) | integration-dev |
| O-AT | Die `unknown`-Grenze an `ExportTemplate.definition`: `lib/exportTemplateModel.ts` packt daraus Feldnamen aus, die ein Benutzer geschrieben hat und die angezeigt werden — der letzte Rest Anzeigefläche, den `proof:foreign` nicht erreicht (T-129 Frage 3) | frontend-dev |

## Blockiert — braucht eine Umgebung, die hier nicht steht

| ID | Aufgabe | Blockiert durch |
|---|---|---|
| T-B02 | Add-in gegen die Referenzbilder | Referenzbilder liegen nicht vor |
| T-B05 | Windows-Prüfliste, jetzt sieben Punkte | Kein Windows-Rechner. Wichtigster Punkt: Takt mit gesetzter Umgebungsvariable starten und prüfen, dass trotzdem der richtige Name im Export landet (B-8.1, E-042). **Neu:** Nach der Installation muss `…\Takt\taskpane\index.html` existieren — dass NSIS Ressourcen nach `$INSTDIR` legt, ist die unbewiesene Annahme, auf der die Auslieferung des Add-ins steht. |
| T-B08 | Die `.AppImage` mit Playwright fahren | Playwright hat auf Linux keinen Anknüpfungspunkt für Tauris Webview — belegt über die Bibliotheksabhängigkeiten und das Fehlen jeder Tauri-Unterstützung in der Schnittstelle, anders als bei Electron. Keine Auslassung, eine Grenze. |
| T-B06 | 42Crunch-Audit und -Scan | `42c-ast` nicht installiert, keine Zugangsberechtigung. **Es gibt keinen Auditwert.** |
| T-B07 | `cargo audit` über den Rust-Baum | Nicht installiert. Der Rust-Anteil ist nie auf Abhängigkeiten geprüft worden. |

## Offene Fragen an den Auftraggeber

| Nr | Frage |
|---|---|
| F-14 | `WindowsUser` im Export — nackter Name oder `DOMAIN\benutzer`? Die Hülle liefert beides, die Stelle ist beschriftet. |
| F-15 | Was steht hinter „Wenden Sie sich an Ihre Systembetreuung"? Der Satz steht an vier Stellen. Arbeitest du allein mit Takt, ist er eine Sackgasse. |
| F-17 | Sollen Pools sortierbar sein (siehe O-B)? |
